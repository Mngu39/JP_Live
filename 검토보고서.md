# 실행 가능성 및 구현 검토

> **최신 상태: [현재상태.md](현재상태.md), audio-timeline-2026-09-10.** 이전 사본은 사용자 iPad에서 컴파일·앱 실행·STT 준비에 성공했으나 스트리밍 timestamp 오류로 실패했습니다. 이번 수정본의 Apple 빌드·72개 XCTest 실행·iPad runtime은 미실행입니다. 아래 이전 검토의 수치/판정은 해당 시점의 기록입니다.

2026-09-07. 이 결과물은 **빌드 전 소스 프로토타입**입니다. Playgrounds/Xcode 프로젝트와 실제 API를 호출하는 소스는 작성했지만, Windows 환경에서 Apple SDK 컴파일이나 iPad 실행을 했다는 뜻은 아닙니다. 서명된 앱/IPA는 없습니다.

이 문서는 최초 실행 가능성 검토 기록입니다. 이후 시간 정렬·DF 우회·번역 재시도·chunk 경계·형태소 비동기 처리 수정의 최신 상태와 검사 개수는 [추가검토결과.md](추가검토결과.md), [검사결과.md](검사결과.md)를 우선합니다.

2026-09-08 정정: 사용자 iPad에서 FluidAudio의 필수 Nemo remote binary ZIP 검증이 unzip 실행 파일 부재로 실패했습니다. 이에 최신 Phase 1은 외부 SPM 0개, diarization 비활성으로 범위를 조정했고 Phase 2에 해당 요구와 의존성을 유지했습니다. Phase 2 선택 모델 준비도 기본 STT/capture 시작 이후로 분리했습니다. 아래 최초 문서의 화자 분석 경로를 최신 Phase 1에서 실행 가능한 기능으로 읽으면 안 됩니다. 새 Apple resolve/compile/run은 아직 미확인입니다.

## 현재 판정

| 구분 | 결과 |
|---|---|
| 작성한 기능 | 파일 입력, Apple STT, 안정된 chunk 번역, Rail/Dock UI, 글자 위치 기반 단어 탭, 두 언어 DeepL 팝업, 일본어 AI 재구성/한자/저장, WebView, 인증 |
| Phase 2 추가 소스 | iOS 27 ScreenCaptureKit 전체 화면 공유 입력, screen-capture background mode, DeepFilterNet 연결 |
| 실행한 검사 | 첨부 Worker를 격리해 API 계약 10개 검사; Swift 문법 파서 검사; Xcode 프로젝트 구조 파서 검사 |
| 실행하지 못한 검사 | Swift 타입 검사/링크, Apple SDK 빌드, XCTest, Playgrounds 실행, iPad 캡처·UI·STT·번역·성능 |
| 명확한 미완료 | Sudachi 바이너리/사전 탑재; 실시간 2화자 분리 및 mixed 자막 대체·병합; 실제 설치 가능한 IPA |

따라서 “전체 구현 완료”, “sideload에서 해결 확인”으로 분류할 항목은 아직 없습니다. 개별 소스 작성과 기기에서의 기능 성공을 구분합니다. 요구사항 0~39는 별도 대장에 모두 남겼습니다.

## 기존 가정의 정정과 근거

1. **Swift Playground와 iPadOS 버전은 별개입니다.** 확인한 Swift Playground 4.7 릴리스는 Swift 6와 iOS 26 SDK를 명시합니다. iPad가 27이어도 이 SDK로 iOS 27 ScreenCaptureKit 심볼을 컴파일할 수 있다고 가정할 수 없습니다. Phase 1은 파일 입력, Phase 2는 iOS 27 SDK 프로젝트로 나눴습니다. [Apple 릴리스 정보](https://apps.apple.com/nz/app/swift-playground/id908519492)
2. **iOS의 ScreenCaptureKit을 전면 불가능하다고 판단하는 것은 맞지 않습니다.** Apple의 iOS 캡처 샘플에는 전체 화면 picker와 background screen-capture 경로가 있습니다. 해당 API에 맞춰 소스를 작성했습니다. 다만 이것만으로 YouTube 공식 앱의 모든 영상 오디오, Windowed Apps의 모든 상태가 성공한다고 결론 내릴 수는 없습니다. 보호 콘텐츠나 앱별 출력 동작은 실기 확인 대상입니다. [Apple iOS 캡처 샘플](https://developer.apple.com/documentation/screencapturekit/capturing-screen-content-on-ios)
3. **시스템 볼륨과 캡처 PCM 크기의 관계는 미확정입니다.** 문서만으로 10/50/100%가 동일하다고 단정하지 않았습니다. 설정에 입력 RMS/peak를 넣었고 실기 절차를 제공합니다. 사용자가 방송마다 옵션을 바꾸도록 설계하지 않았습니다.
4. **Apple STT 두 인스턴스를 API가 원천 금지한다고 볼 근거는 없습니다.** 기본 STT와 별도의 두 인스턴스 실험 코드를 작성했습니다. 두 스트림에서의 정확도·처리량·지연은 별개이며 미검증입니다. [SpeechAnalyzer](https://developer.apple.com/documentation/speech/speechanalyzer), [Apple 음성 인식 샘플](https://developer.apple.com/documentation/speech/bringing-advanced-speech-to-text-capabilities-to-your-app)
5. **명시적 lowLatency 전략은 SDK 가용성 분기가 필요합니다.** 공식 메타데이터에서 해당 전략 생성자는 iOS 26.4 도입입니다. Phase 1은 기존 생성자를 사용하고, Phase 2는 `.lowLatency`를 명시합니다. Phase 1의 전략이 명시적 lowLatency와 같다고 주장하지 않습니다. [TranslationSession](https://developer.apple.com/documentation/translation/translationsession)
6. **DeepFilterNet CoreML이라는 이름만으로 패키지 전체가 Core ML 단독이라고 볼 수 없습니다.** 검토한 배포본은 Swift tools 6.2를 요구하고 MLX 관련 의존성이 남아 있습니다. Phase 2에만 연결했습니다. 실제 설치 크기·호환성·전력은 빌드/실기에서 확인해야 합니다. [검토한 DeepFilterNet 패키지](https://github.com/kylehowells/DeepFilterNet-mlx)
7. **음악의 보컬/반주 분리와 두 사람의 음성 분리는 다릅니다.** 일반 음악 분리 모델을 요구된 2-speaker separation으로 대체하지 않았습니다. SpeechBrain SepFormer 후보용 변환 스크립트는 실험용이며 변환 성공을 확인하지 않았습니다. [SpeechBrain SepFormer 모델](https://huggingface.co/speechbrain/sepformer-whamr16k)
8. **1W/1~2W는 목표이며 결과가 아닙니다.** M4라는 이유만으로 모델 여러 개의 장시간 동작을 보증하지 않습니다. [FluidAudio 구현](https://github.com/FluidInference/FluidAudio)
9. **Sudachi의 iOS 지원과 바로 쓸 수 있는 완성 패키지는 다릅니다.** sudachi.rs의 embedded 경로를 사용한 Rust/C 연결 소스를 작성했지만 사전과 XCFramework는 별도로 빌드·연결해야 합니다. 현재 Apple 분석기 fallback을 Sudachi와 동등하다고 취급하지 않습니다. [sudachi.rs](https://github.com/WorksApplications/sudachi.rs)

## 실제 파이프라인과 남은 차이

현재 기본 경로는 입력 → 48kHz 변환·짧은 stereo 원본 보관 → 역위상 취소 방지 mono 선택 → 비동기 화자 분석의 최근 확률 → 선택적 음성 개선 → 음성 확률 기반 leveling/limiter → Apple STT → 확정 chunk → Apple 번역입니다.

원본 stereo는 12초 ring에 보관하지만, 현재 STT용 stereo 활용은 역위상 보호와 평균 처리까지입니다. center 음성/BGM 공간 분리까지 구현했다고 보지 않습니다. 실제 소스에서는 enhancement 다음 leveling 순서이며, 원 요구의 앞단 leveling과 stem별 leveling 전체 구조를 완성한 상태가 아닙니다.

LS-EEND 분석은 기본 STT를 기다리게 하지 않도록 별도 큐에서 처리합니다. 분석 실패/큐 지연 시 기본 STT를 계속하고 상태를 표시합니다. 확률이 없거나 오래되면 조용한 BGM을 음성으로 추정해 증폭하지 않습니다. 다만 모델 출력의 지연 보정과 PCM 구간별 정밀 매칭은 더 필요합니다. 현재 recent-decision 방식은 정확한 VAD 정렬이나 화자별 증폭 검증을 대신하지 못합니다.

DeepFilterNet은 출력 지연을 고려해 dry/wet을 맞추고, 한 명으로 판단될 때 보수적으로 35%를 섞도록 작성했습니다. 이는 완성된 잡음 적응 알고리즘이 아닙니다. 깨끗한 음성의 손상 여부, BGM 억제 효과, 모델 오류 전환 시 짧은 경계 손실은 아직 검증하지 않았습니다.

2화자 분리는 모델 입출력 검사, 정확히 2명일 때만 여는 gate, 두 STT 실험 소스까지만 있습니다. 모델 가중치, 실시간 overlap 구간 추출, stem별 음성 판정·증폭, 인접 창의 stem 순서 정렬, 중복 제거, mixed 자막 대체가 없습니다. **모델 파일만 넣으면 실시간 분리가 켜지는 구조가 아닙니다.** 현재 앱은 겹침 구간에서도 기본 mixed STT를 사용합니다. 3명 이상 분리는 추가하지 않았습니다.

chunk는 확정 결과와 문장부호/길이/휴지기 휴리스틱으로 묶습니다. volatile 결과마다 번역을 요청하지 않습니다. 형태소의 연결 어미, 화자 전환, 정렬된 VAD를 종합한 최종 경계 알고리즘은 부분 구현입니다. 내부 길이 분할에는 상위 발화 시간 구간을 유지하며 임의의 단어 타임스탬프를 만들지 않습니다.

## 기존 일본어 앱 연동

첨부 ZIP의 코드와 데이터가 검토 기준입니다. 배포 서버의 최신 버전과 같다는 보증은 하지 않습니다.

- DeepL: 기존 `/run/translate`, 일본어 JA/영어 EN → KO. 선택한 단어는 lemma를 보냅니다. 실시간 전체 자막은 이 경로를 쓰지 않습니다.
- AI 재구성: 일본어에서 기존 `/run/restructure`; 원형 형태소를 보존하고 재구성 단위와 전환합니다. UTF-16 범위 겹침으로 선택 위치를 대응합니다. 기존 웹과의 시각적 동등성은 미검증입니다.
- 저장: `/api/save`는 image/bbox 없이 문장/단어 저장을 허용함을 격리 검사했습니다. 일본어만 저장하도록 앱에서 제한합니다. 각 run+chunk에 고유 context_group_id를 주며 그 문장/단어는 같은 group을 씁니다.
- 인증: 기존 APP_TOKEN을 Keychain에 보관하고 `/auth/open-token`에서 단기 log token을 발급합니다. 만료 전에 갱신하고 401에 한 번 재발급합니다. 저장 네트워크 오류는 임의 재전송하지 않아 중복 저장 위험을 줄입니다.
- 단어장: 기존 logs.html을 앱 안 WKWebView로 표시합니다. APP_TOKEN을 웹에 전달하지 않고 단기 log token만 사용합니다.
- 데이터: 기존 deck/한자 JSON을 재사용했습니다. deck 인덱스는 2,306개 항목입니다. 새 단어장 DB를 만들지 않았습니다.

### 확인된 Worker 문제

첨부 Worker의 여러 dispatcher 분기는 `try` 안에서 async handler를 `return handle…()`로 반환하고 있습니다. 여기서 나중에 발생한 rejection은 바깥 `catch`가 처리하지 못합니다. 실제로 격리 검사에서 잘못된 인증/입력은 지정된 HTTP 응답 대신 Promise rejection으로 나타났습니다. 운영 환경에서는 정상 JSON 401/400 대신 일반 오류가 될 수 있어 앱의 401 재발급 경로가 작동하지 않을 가능성이 있습니다. 운영 서버에서 재현했다고 주장하지는 않습니다.

해결은 해당 async handler 반환을 `return await handle…()`로 바꾸어 catch가 받아 응답을 만들도록 하는 것입니다. 정상 번역·저장 계약을 바꿀 필요는 없습니다. 기존 Worker는 수정/배포하지 않았습니다. 자동 사전 갱신은 평상시 만료를 줄이지만 이 서버 오류를 완전히 대신하지 못합니다.

## 다음 구현 우선순위

1. macOS/iOS 27 SDK에서 최초 빌드 및 iPad의 YouTube PCM 수신을 확인합니다. 이 단계가 최종 용도의 가장 큰 불확실성을 해소합니다. SideStore는 빌드 도구가 아니므로 Xcode 호스트가 필요합니다. [SideStore 준비 사항](https://docs.sidestore.io/docs/installation/prerequisites)
2. Sudachi 사전/바이너리를 연결하고 실제 조사·활용형·고유명사에 대한 lemma/reading과 탭 범위를 확인합니다.
3. 화자 분석 시간을 PCM과 정렬하고, 깨끗한 음성과 방송 혼합음으로 전처리가 STT를 실제 개선하는지 비교합니다. 사용자에게 매 방송 수동 설정을 요구하는 기능은 추가하지 않습니다.
4. 2화자 모델 변환·정확도·처리량을 먼저 검증한 후 overlap 경로를 연결합니다. 기본 결과를 먼저 보여주고 분리 결과 확정 시 해당 시간 구간만 교체하는 방식이 유력합니다. 실패하면 기존 결과를 유지해야 합니다.
5. 장시간 자막·번역 큐의 메모리 증가를 측정하고 필요 시 로컬 기록을 페이지 단위로 읽도록 개선합니다. 현재 transcript 전체는 메모리에 있으므로 수십 시간 규모를 보장하지 않습니다.

이는 결정되지 않은 UI 옵션을 사용자에게 떠넘기는 목록이 아닙니다. 알고리즘 기본값은 구현자가 정하고, 현재 없는 Apple 빌드 환경과 실제 기기 측정이 필요한 부분을 분리한 것입니다.
