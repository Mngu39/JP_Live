# 추가 검토 및 외부 지적 반영

> **최신 상태: [현재상태.md](현재상태.md), audio-timeline-2026-09-10.** 이전 사본은 사용자 iPad에서 컴파일·앱 실행·STT 준비에 성공했으나 스트리밍 timestamp 오류로 실패했습니다. 이번 수정본의 Apple 빌드·72개 XCTest 실행·iPad runtime은 미실행입니다. 아래 이전 검토의 수치/판정은 해당 시점의 기록입니다.

## 최신: LanguageTools 실제 compiler 오류 3건 — 2026-09-08

사용자 보고상 이전 수정본은 dependency 단계를 통과한 뒤 Apple compiler에서 아래 diagnostics가 발생했습니다. 추가로 지정한 이미지 경로는 로컬에서 파일을 찾지 못해 화면 자체는 확인하지 못했으며, 전달받은 오류 원문과 현재 소스를 기준으로 수정했습니다.

1. `Cannot convert value of type 'CFString' to expected argument type 'CFLocaleIdentifier'`: `CFLocaleCreateCanonicalLocaleIdentifierFromString`으로 식별자를 만든 뒤 `CFLocaleCreate`에 전달합니다. 생성 실패 시 기존 읽기 fallback의 빈 값을 반환하며 강제 언래핑하지 않습니다. [Apple의 locale 식별자 API](https://developer.apple.com/documentation/corefoundation/cflocale?changes=__6)
2. `Cannot infer contextual base in reference to member 'latinToHiragana'`: 호출을 `StringTransform.latinToHiragana`로 명시했습니다. [Apple API](https://developer.apple.com/documentation/foundation/stringtransform/latintohiragana)
3. `Type 'Bundle' has no member 'module'`: LearningData에서 생성 accessor 참조를 제거하고 `Bundle.main`의 JSON을 읽습니다. root와 Resources 하위 디렉터리를 우선 확인하고, 호스트가 별도 resource bundle로 넣었을 경우 앱 내부의 바로 아래 bundle에서 찾습니다. 생성된 bundle 이름을 가정하거나 앱 외부 파일을 찾지 않습니다. 일반 SwiftPM resource accessor 생성과 이 사용자 App Playground의 오류는 구분합니다. [Apple의 패키지 리소스 설명](https://developer.apple.com/documentation/xcode/bundling-resources-with-a-swift-package)

설정에 ‘학습 데이터 확인’을 추가했습니다. 팝업이 사용하는 것과 같은 한 번 해석한 JSON 데이터에서 로드 건수 또는 실패 이유를 표시하므로, 파일 존재만 보고 정상이라고 표시하지 않습니다. 파일 없음·읽기 실패·형식 오류·빈 데이터를 구분합니다. 원본 JSON에서 확인한 기대 건수는 **한자 사전 5663, 단어장 인덱스 2306**이며, 실제 기기에서 이 값이 보이는지 확인해야 합니다.

Phase 2는 원래 Bundle.main을 사용하는 앱 target이며 두 JSON이 Copy Bundle Resources에 연결되어 있음을 대조했습니다. Xcode 프로젝트·Phase 1 manifest·오디오/STT/번역/화자 처리 코드는 이번에 변경하지 않았습니다. Sudachi는 계속 미연결이며 그 비활성 분기의 리소스 코드를 변경하지 않았습니다.

변경 Swift 2개와 복사본 2개, hosted XCTest 1개의 문법을 확인했습니다. Shared 16개 및 JSON 복사본 일치, Phase 2 리소스 target 포함, JSON 구조·원본 건수를 확인했습니다. XCTest 소스는 앱 번들의 실제 JSON decode 검사를 1개 추가해 총 55개이고 **실행은 0개**입니다. Windows 정적 검사와 ZIP 대조는 앱 내 JSON 로드 성공을 대신하지 않습니다.

다음 관문은 최신 Playground 사본의 실제 compile→run→설정에서 두 데이터 로드 확인입니다. 새 compiler 오류는 첫 오류 원문을, JSON 오류는 ‘학습 데이터 확인’의 표시 내용을 보존해 이어서 수정합니다.

## 이전: Playground host 의존성 실패 및 선택 모델 시작 순서 — 2026-09-08

사용자가 제공한 이전 ZIP의 실제 iPad 오류 원문은 다음과 같습니다. 앱 로직 실행 전 binary target archive 검증에서 실패한 기록이며, OS/Playgrounds 정확한 버전은 이번 오류 메시지에 포함되지 않았습니다.

```text
Failed validating archive from
'[https://github.com/FluidInference/text-processing-rs/releases/download/v0.3.0/NemoTextProcessing.xcframework.zip](https://github.com/FluidInference/text-processing-rs/releases/download/v0.3.0/NemoTextProcessing.xcframework.zip)'
which is required by binary target 'NemoTextProcessing':
could not find executable for 'unzip'
```

고정한 [FluidAudio manifest](https://github.com/FluidInference/FluidAudio/blob/5c19d5e12320e22bbfb7a1877b089d2665a69add/Package.swift)의 FluidAudio target은 NemoTextProcessing을 필수로 참조하고 해당 remote binary는 ZIP으로 제공됩니다. 따라서 이 host 도구 의존성을 이전 검토에서 별도 실검증 대상으로 분류했어야 했습니다. 이번 오류를 모든 Playgrounds 버전에서 unzip이 없다는 일반 주장으로 확대하지 않습니다.

### Phase 1: 외부 SPM 제거

`JPLive.swiftpm/Package.swift`의 FluidAudio package와 product dependency를 모두 제거했습니다. package/target dependency 배열은 비어 있으며, remote binary·build plugin·system-library target·다른 로컬 패키지를 연결하지 않았습니다. 기존 canImport 분기를 이용하므로 실제 FluidAudio adapter는 Phase 1에서 컴파일 대상에서 빠집니다. 앱에 가짜 diarizer/VAD를 넣지 않았고, Rust/Cargo 빌드도 manifest에서 실행하지 않습니다.

AppleProductTypes는 Playground 호스트가 제공하는 manifest 모듈입니다. SwiftUI/AVFoundation/Speech/Translation 등은 Apple SDK 프레임워크입니다. Apple 음성·번역 언어 모델 다운로드는 런타임 준비이며 SwiftPM archive 해제와 구분합니다. 이 구조 확인을 실제 Apple resolve·타입 검사·compile 성공으로 표현하지 않습니다.

Phase 1은 화자 분석 없는 기본 STT 경로이므로 화자별 증폭, 확인된 화자 전환 경계와 색 힌트 검증을 제외합니다. 기본 STT·번역·철회·문장/단어 팝업·일본어 학습 연결이 우선 범위입니다. diarization 요구는 유지하며 Phase 2 FluidAudio 연결도 그대로입니다.

### Phase 2: 기본 입력을 먼저 시작하고 준비된 모델을 늦게 연결

이전 AppModel은 FluidAudio→DeepFilter→Apple STT→입력 시작을 직렬로 기다렸습니다. 이제 필수 Apple STT 준비→입력 시작/picker→첫 PCM의 기본 STT 전달까지 선택 모델 없이 진행합니다. 그 뒤 FluidAudio와 DeepFilter 준비를 각각 독립된 낮은 우선순위 작업으로 시작합니다. 필수 Apple Speech 준비/언어 모델 다운로드 자체는 여전히 입력 시작 조건이며, 이를 생략한 변경은 아닙니다.

준비 작업은 capture마다 별도 mailbox에 결과만 넣습니다. 완료 콜백은 AppModel이나 live pipeline을 직접 변경하지 않고, 단일 입력 루프가 다음 chunk 경계에서 준비된 provider를 연결합니다.

- 늦게 준비된 분석은 그 다음 PCM 시각을 새 원점으로 사용합니다. 이전에 이미 STT로 보낸 오디오·자막을 소급 분석하지 않습니다. 연결 뒤 finalized 결과를 기다리는 기존 2초 대기 예산은 유지합니다.
- 늦게 준비된 enhancer는 설치 시점의 입력 종료 시각 이후 오디오에만 적용합니다. 분석을 기다리는 과거 PCM까지 갑자기 새 enhancer로 처리하지 않습니다. enhancer가 먼저 준비돼도 단일 화자 판단이 없으면 원본 경로를 유지합니다.
- 중지/오류/EOF에서는 mailbox를 닫고 준비 작업을 취소합니다. 다운로드 완료를 기다리지 않고 필수 입력/STT 정리를 진행하며, 이전 capture의 늦은 결과는 다음 capture에 연결되지 않습니다. 외부 라이브러리 내부 다운로드가 취소 요청에 실제로 얼마나 빨리 반응하는지는 실기 검증 대상입니다.
- 선택 모델 준비 실패는 상태만 표시하고 기본 STT를 종료하지 않습니다. 실제 입력 지연·발열·메모리·준비 중 중지/재시작은 기기에서 확인해야 합니다.

### Phase 2 의존성 그래프와 버전 고정

[고정한 DeepFilter manifest](https://github.com/kylehowells/DeepFilterNet-mlx/blob/c6c4b9dc98c62bfa90ed66c5fd4f5d54e88c7b4f/Package.swift)에서 CoreML target은 DeepFilterNetMLX·MLX·HuggingFace를 참조합니다. 패키지 의존성에는 mlx-swift, swift-huggingface, swift-argument-parser가 있습니다. ArgumentParser의 앱 링크 여부와 패키지 그래프 해석은 구분하며, Core ML 단독 의존성으로 보지 않습니다. 기본 모델 source는 bundle을 먼저 보고 없으면 Hugging Face snapshot을 받는 [.automatic 구현](https://github.com/kylehowells/DeepFilterNet-mlx/blob/c6c4b9dc98c62bfa90ed66c5fd4f5d54e88c7b4f/Sources/DeepFilterNetCoreML/DeepFilterNetCoreML.swift)입니다.

최초 Xcode resolve 성공 후 `Phase2/JPLive.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved`를 프로젝트에 보존합니다. 빌드 스크립트는 실제 생성 파일을 확인하고 `BuildOutputs/Package.resolved`에도 복사합니다. 기존 lockfile을 사용하는 resolve와 archive에는 `-disableAutomaticPackageResolution`을 넣고 archive 중 파일 변경 여부를 검사합니다. 이는 [Apple의 Swift 패키지 빌드 지침](https://developer.apple.com/documentation/xcode/building-swift-packages-or-apps-that-use-them-in-continuous-integration-workflows)에 따른 절차입니다. **Xcode resolve·스크립트 실행 전이므로 실제 전이 버전 고정은 아직 미완료**이고 임의 lockfile은 제공하지 않습니다.

### 이번 검증 범위와 다음 관문

Swift 문법·프로젝트 참조·복사본의 기존 자동 검사에 Phase 1 의존성 부재, Phase 2 고정 커밋 유지, 선택 모델 시작 순서와 늦은 연결, lockfile 보존 절차의 소스 검사를 추가했습니다. 이 자동 검사는 Apple 실행 검사가 아닙니다. 회귀 XCTest 소스 4개를 추가해 총 54개이며 실행은 0개입니다. 준비가 멈춰 있어도 기본 PCM/중지가 진행되는 경우, 늦은 분석의 시각 원점, 늦은 enhancer의 과거 대기 PCM 제외, enhancer가 먼저 준비되는 순서를 다룹니다.

새 Playground ZIP을 새 폴더에 풀어 의존성 확인→Apple compile→run을 다시 수행해야 합니다. 같은 Nemo 오류가 보이면 이전 Playground 사본을 열었는지 먼저 확인하고, 새 오류이면 첫 오류 원문·OS/Playgrounds 버전을 남깁니다. 전체 normalization·Sudachi·live 2화자 separation/stem leveling/STT 병합·장시간 처리·영어 문맥 번역은 기존 미완료 상태입니다.

## 이전: 단일 화자 교체 시 gain 상속 수정 — 2026-09-08

최신 첨부에서 지적한 A→B gain 상속 한 건을 수정했습니다. 이전 코드는 두 구간 모두 단일 화자이면 같은 envelope를 이어 사용해 작은 A의 증폭이 큰 B의 첫 frame에 남을 수 있었습니다.

`AudioPipeline.render`가 해당 finalized 오디오 구간의 단일 화자 slot을 `SpeechLeveler`에 전달합니다. Leveler는 증폭 허용·음성 확률 0.65 이상·알려진 slot 조건에서 마지막 알려진 slot과 비교하고, 서로 다르면 **gain을 1로 초기화한 뒤 새 frame의 RMS와 target gain을 계산**합니다. UI gutter hint는 이 판단에 사용하지 않습니다. 이미 출력된 순서의 오디오를 기준으로 처리하므로 enhancer가 지연해서 내보낸 frame에도 같은 기준을 적용합니다.

단일 화자이지만 slot이 nil이거나 음성 확률이 낮으면 화자 ID 변경에 따른 초기화나 마지막 알려진 slot 교체를 하지 않습니다. 따라서 A→unknown→A나 낮은 확률의 다른 slot 때문에 새 화자라고 판단하지 않습니다. 기존의 overlap/분석 없음/무음에 대한 증폭 차단은 유지합니다. 이 조건은 현재 finalized 분석을 이용하는 코드 기준이며 실제 diarizer의 사람 구분 정확도를 보증하지 않습니다.

회귀 XCTest 소스 2개를 추가해 총 50개입니다. 첫 검사는 quiet A→loud B에서 B의 첫 샘플부터 이전 gain이 없어야 함을 enhancer 없는 경로와 출력이 지연되는 경로 양쪽에서 확인하도록 작성했습니다. 둘째는 unknown/낮은 확률 slot이 초기화나 ID 교체를 유발하지 않는지 비교합니다. **실행한 XCTest는 0개**이며 Apple 환경에서 수행해야 합니다.

이번 검사는 변경 Swift 2개와 복사본 2개, XCTest 1개의 문법 및 소스 연결 확인, Shared/리소스 복사본과 ZIP 내용 대조에 한정했습니다. 추가 전수 정적감사는 수행하지 않았습니다. 전체 normalization·Sudachi·live 분리·장시간 처리·영어 문맥 번역의 기존 미완료 상태를 유지합니다. 다음 관문은 iPad에서 `JPLive.swiftpm` 의존성 resolve → compile → run입니다. 오류가 발생하면 첫 compiler/package 오류 원문과 Playground/OS 버전을 보존해 수정해야 합니다.

## 이전: overlap 증폭 차단 및 soft gutter — 2026-09-08

최신 첨부 요청의 1~2번만 소스 수정하고, 3번과 추가 backlog는 미완료 요구사항으로 명시했습니다. 일반적인 전수 정적 재검토를 반복하지 않습니다. 다음 관문은 **iPad Swift Playgrounds에서 의존성 resolve → 최초 compile/run**이며, 현재는 여전히 Apple 빌드 전 alpha 소스입니다.

### overlap mixed 신호의 증폭 차단

`AudioPipeline.render`는 해당 오디오 구간의 `activeSpeakers == 1`일 때만 SpeechLeveler의 upward gain을 허용합니다. 2명 이상·미확인·무음에서는 확률을 음성 증폭에 전달하지 않고 gain을 즉시 1로 되돌립니다. 확률만 nil로 바꾸면 이전 단일 화자 구간의 gain이 완만히 내려가는 동안 mixed 신호가 계속 증폭될 수 있어 그 잔여 gain도 제거했습니다. compressor/limiter는 유지합니다. DF도 기존과 같이 단일 화자에서만 실행합니다.

이것은 separator를 연결한 변경이 아닙니다. 겹침은 증폭 없는 mixed STT fallback이며, 분리 후 각 stem의 음성 판정·독립 leveling은 여전히 미구현입니다. 전체 입력 normalization 역시 별도로 구현해야 합니다.

### 내부 화자 ID와 보조 gutter 분리

`Caption.speaker`는 시간 구간 분석과 chunk 경계의 내부 ID로 유지하고, `gutterHint`는 별도 `SoftSpeakerMapper`에서 결정합니다. raw slot의 `% 2` 색 선택을 제거했습니다. 주 화자와 보조 후보/화자 각각 하나만 보관하며, 계속 증가하는 사람별 색 목록은 만들지 않습니다.

- 임시 STT 행, 단독 발화, 아직 확인되지 않은 보조 후보, unknown, 최근 두 화자 사이에 끼어든 제3 화자는 중립 표시입니다.
- 확정 chunk에서 서로 겹치지 않는 시간 증거를 누적합니다. 최초 보조 색 확인 기준은 각 화자 2회 이상 관측·누적 1.2초 이상입니다. 같은 시간 구간의 길이 분할/중복 행은 횟수를 부풀리지 않습니다.
- 보조 후보는 마지막 관측 뒤 3초, 확인된 보조 화자는 6초, 주 화자는 12초가 지나면 만료합니다. 최근 두 화자의 A→B→A에서는 기존 보조 색을 유지하며 최근 발화자가 주 화자로 바뀝니다. 만료 후 단독 발화는 다시 중립입니다.
- 과거 행의 색은 나중에 바꾸지 않으며 새 입력마다 mapping을 초기화합니다. 짧은 응답이 한 번뿐이면 보조 화자 색을 확정하지 않습니다. 이 숫자는 초기 보수적 기준이며 실제 사람 identity나 정확도를 보증하지 않습니다. A/B/NPC 음원으로 표시 지속 시간·확인 문턱을 실기에서 조정해야 합니다.

### 검사 범위와 미완료 사항

변경한 Swift 소스 6개와 Playground 복사본 6개, XCTest 소스 1개의 tree-sitter 문법을 확인하고 Shared 복사본 및 ZIP 내용을 대조합니다. 이번에 XCTest 9개를 추가해 총 48개 소스이며 **실행한 XCTest는 0개**입니다. 겹침 전환의 잔여 gain·limiter·DF 우회와 단독/0↔2/제3/unknown/만료/중복 시간/입력 재시작 표시를 다룹니다. Apple SDK 타입 검사·컴파일·화면 실행은 수행하지 못했습니다.

전체 입력 normalization, history를 보존하는 장시간 배열 처리 최적화, 영어 단어 팝업의 현재 caption 문맥 번역은 [요구사항대장.md](요구사항대장.md)에 작업 시점과 완료 기준을 기록했습니다. Sudachi 및 live 2화자 분리 미완료도 유지합니다. 기존 Worker·GitHub·GCP 수정/배포와 운영 API 호출은 하지 않았습니다.

## 이전: 두 번째 외부 검토의 1~4번 및 형태소 처리

첨부한 검토문과 현재 코드를 대조해 네 경로의 실제 문제를 확인하고 소스를 수정했습니다. 이번 검사는 변경한 경로의 회귀 검사와 패키징에 한정하며, 다음 통과 기준은 **Phase 1 실제 Apple 컴파일·실행**입니다. 빌드 성공이나 실기 오류 없음으로 판정하지 않습니다.

### 1. 지연된 화자 분석과 원본 오디오의 대응

이전 코드는 마지막 finalized 확률 하나에 누적 종료 시각을 붙이고, 현재 입력과 0.5초 이상 다르면 버렸습니다. 이제 `DiarizerTimelineUpdate.chunkResult`의 `startFrame`, `finalizedFrameCount`, 프레임 주파수로 모든 finalized 프레임의 **시작·끝 오디오 시각**을 복원합니다. 한 번 도착한 여러 프레임을 마지막 하나로 줄이지 않습니다. 모델 재설정과 EOF에서도 실제 입력 길이와 원점을 보존합니다.

`SpeechTimeline`은 해당 구간 전체에 대응하는 판단만 반환합니다. 과거/미래 판단이나 분석 공백을 현재 프레임에 대입하지 않습니다. `AudioPipeline`은 원본 모노를 10ms 구간으로 보관해 대응 결과를 기다립니다. 입력 처리 때 오디오 진행·실제 대기를 확인하는 예산은 각각 2초이며, 초과·큐 넘침·분석 오류이면 경고를 남기고 기본 STT 경로로 보관된 오디오를 넘깁니다. 입력 이벤트 자체가 멈추면 다음 입력 또는 종료에서 잔여분을 처리하므로 독립 타이머에 의한 강제 2초 방출은 아닙니다. 이 예산은 검증된 최적 지연값이 아니며 실기 측정이 필요합니다.

음량 보정과 enhancement gating은 이렇게 대응시킨 구간의 확률을 사용합니다. 출력 오디오에는 적용한 판단을 함께 전달하고, AppModel도 같은 이력에서 STT 구간의 화자를 조회합니다. 알려진 단일 화자 사이의 final STT 결과 전환에서는 pending chunk를 구분합니다. unknown/겹침은 분할 근거로 쓰지 않습니다. 한 STT 결과 내부를 단어 시각으로 나누는 기능은 아직 없습니다.

외부 설명의 “900ms tentative preview”는 고정한 commit에 그대로 적용되지는 않습니다. [해당 LSEENDDiarizer 구현](https://github.com/FluidInference/FluidAudio/blob/5c19d5e12320e22bbfb7a1877b089d2665a69add/Sources/FluidAudio/Diarizer/LS-EEND/LSEENDDiarizer.swift)은 이 경로에서 tentative 배열을 비워 전달합니다. 수정은 특정 900ms 값을 가정하거나 freshness를 늘리는 방식에 의존하지 않고, 실제 프레임 시각을 사용합니다.

### 2. 사용하지 않는 DeepFilter 추론 우회

일치하는 단일 화자 구간에서 실제 35% 개선 신호를 사용할 때만 enhancer `process`를 호출합니다. 미확인/침묵/겹침 등 wet=0 구간에는 `processHop` 또는 `flush`를 호출하지 않습니다. wet→dry 경계에서는 아직 출력하지 않은 원본 tail을 자체 원장으로 회수하고 모델 상태를 초기화한 다음, 현재 dry 구간을 이어 보냅니다. 모델 출력은 원래 입력의 시각·확률 순서로 분배해 음량 보정합니다.

wet 재진입은 초기화된 상태에서 시작하므로 이전 구간의 recurrent state를 이어 쓴 것처럼 취급하지 않습니다. EOF의 실제 wet tail만 flush합니다. 오류나 출력 누락도 원본 원장으로 복구하며, 출력 대기 원장은 1초분으로 제한했습니다. 초기에 모델을 로드하는 비용은 여전히 있고, 전력 절감량·재진입 품질은 미측정입니다. 별도의 소음 종류 판정까지 완성했다는 의미는 아닙니다.

### 3. 개별 자막 번역 실패 복구

번역 backlog에 시도 상태와 다음 시도 시각을 보관합니다. 최초 실패 후 1초, 다음 실패 후 3초 대기하여 **최대 두 번 추가 시도**합니다. 대기 항목 뒤의 다른 행과 다른 언어를 막지 않고, 작업 취소·언어 전환은 실패 횟수로 소비하지 않습니다. 세 번째 실패 뒤에는 자동 시도를 멈추고 메뉴의 ‘실패한 번역 재시도’로 사용자가 새 제한 주기를 시작할 수 있습니다. 준비 실패용 ‘번역 다시 연결’과 별도로 동작합니다.

형태소 결과 도착으로 행 revision이 달라져도 원문·capture가 같으면 진행 중 번역 결과를 적용하도록 했습니다. 이전 번역 session의 결과는 기존 revision 검사로 배제합니다. 운영 Worker의 저장 재전송 정책이나 API는 바꾸지 않았습니다.

### 4. final 수신 타이머를 실제 침묵으로 취급하던 경로

`tick`은 현재 오디오 구간에 대응하는 발화·연속 침묵과 최근 STT 변경을 함께 봅니다. 알려진 발화가 이어지거나 volatile continuation이 계속 바뀌면, final 수신 후 1.2초가 지났다는 이유만으로 분할하지 않습니다. 안정된 prefix와 오래된 volatile tail이 함께 있을 때는 해당 tail 뒤의 실제 침묵을 확인한 경우에만 prefix를 내보내며 tail은 보존합니다.

문장부호·일본어 접속형·최대 길이와 9초 제한도 유지합니다. 길이/시간 상한은 발화가 이어져도 final prefix를 내보낼 수 있는 명시적인 지연 제한이며, 침묵 판정과 구분합니다. 분석이 없을 때에는 **volatile 없는 STT idle fallback**을 사용하고 실제 VAD 침묵이라고 설명하지 않습니다. 정밀 문법·형태소 기반 chunk 분석은 여전히 부분 구현입니다.

### 추가: 형태소 처리를 UI 작업에서 분리

TranscriptBuffer는 원문·행 상태만 갱신합니다. 별도 `MorphologyWorker` actor가 일본어/영어 분석을 직렬 처리하고, 최대 128개·각 1,000 UTF-16 이하 원문의 결과를 캐시합니다. volatile 분석은 150ms 동안 변경을 합치고 오래된 요청을 취소하며, 적용 전 행 ID·capture·언어·원문·요청 ID를 확인합니다. 같은 원문에 대한 진행 중 요청은 타이머마다 취소하지 않습니다.

팝업도 자신의 고정 원문에 맞는 분석을 별도로 기다리며, 기본형 읽기를 화면 body에서 동기 계산하지 않습니다. 단어 분석이 준비되지 않은 짧은 시간에는 단어 탭/후리가나가 아직 없을 수 있습니다. 일본어 저장·AI 재구성은 분석 준비 뒤 가능하도록 했습니다. 이는 Sudachi 바이너리·사전 연결을 완료한 것이 아닙니다.

### 검사 및 남은 통과 기준

- Swift 35개(Shared 16개 및 Playground 복사본 포함), Rust 1개, Python 1개, shell 2개 문법 파싱 및 정적 검사 8개 그룹 통과. Apple 타입 검사 아님.
- XCTest는 기존 23개 중 일부 기대값을 갱신하고 16개를 추가해 **39개 소스**. 실제 실행 0개.
- 지연 분석의 원본 시각 적용, 분석 공백·서로 다른 화자, DF 호출 횟수·tail·짧은 EOF, 2초 fallback, 번역 retry 한도·간격·수동 요청·취소, 발화·침묵·volatile·최대 길이를 검사하도록 작성.
- 10ms 출력 프레임으로 바뀐 데 맞춰 STT 입력 큐를 512개로 조정. 정상적인 2초 대기분 방출을 기존 40개 큐가 즉시 거부하지 않게 했으며, 실제 처리 여유와 버스트 수신은 실기 검사 대상.
- Shared/Playground 동기화 및 전체 ZIP의 모든 내용 대조. 최신 파일 수와 해시는 `archive-verification.json` 참조.

Apple SDK·Mac 호스트가 없어 첫 컴파일·XCTest·실기 실행은 아직 수행하지 못했습니다. 이 수정 이후에는 일반적인 전수 정적 검토를 또 반복하는 것을 다음 목표로 삼지 않습니다. Phase 1을 Swift Playground에서 열어 첫 compiler/package 오류를 해결하고 일본어·영어 파일 STT→번역→철회→문장/단어 팝업을 실행해야 합니다. 2화자 live separation·stem VAD/leveling·Sudachi 연결은 여전히 별도 구현이 남습니다.

## 외부 검토 후 수정 — 2026-09-07

외부 평가의 핵심 판단에 동의합니다. 현재는 **pre-build alpha 소스**이며 사용 가능한 앱으로 판정할 단계가 아닙니다. 아래 변경으로도 Apple 최초 컴파일·실행을 대신하지 않습니다. 기존 두 차례 검토 기록은 아래에 이력으로 남깁니다.

### 확인한 지적과 조치

- **빈 STT 결과 철회 누락:** Apple의 [SpeechTranscriber.Result.text 설명](https://developer.apple.com/documentation/speech/speechtranscriber/result/text)은 빈 문자열이 volatile 구간의 이전 결과를 철회할 수 있다고 명시합니다. 기존 `TranscriptBuffer.receive`의 빈 문자열 무시를 수정했습니다. 시각 검사를 통과하고 현재 임시 구간과 겹치거나 시작이 같은 빈 결과는 해당 임시 문구를 지웁니다. 확정 prefix가 있으면 같은 행 ID로 남기고, 임시 문구만 있었으면 행도 제거합니다. 빈 final도 처리하며 확정 이력과 무관한 구간은 보존합니다. 현재 단일 임시 phrase 구조에서 구간이 겹치면 임시 phrase 전체를 철회합니다. 한 phrase의 일부만 철회할 때 나머지 단어를 정확히 보존하는 세밀한 매핑은 제공하지 않습니다.
- **gutter 높이:** 44pt 고정 버튼을 제거하고 caption 내용으로 높이가 정해지는 overlay로 옮겼습니다. 보이는 선 3pt, 터치 폭 30pt를 유지하며 긴 행 전체 높이를 사용하도록 작성했습니다. 실제 SwiftUI 배치·탭 검사는 미실행입니다.
- **화자 전환 chunk 경계:** 지적대로 미완료입니다. `receive`의 speaker는 표시용이고 현재 코드에는 전환 분할이 없습니다. 더구나 AppModel은 STT 구간 시각에 맞춘 이력 대신 최근 색 힌트를 넘깁니다. 이 값을 즉시 비교해 강제 분할하는 변경은 적용하지 않았습니다. 오디오 시각별 단일 화자 이력 → STT 구간에 대응 → 확인된 전환에서 확정 pending 분리 순서가 필요합니다. unknown·겹침은 강제 분할 근거로 쓰지 않습니다.
- **분리 후 작은 stem 보정:** `speechProbability: nil`이면 현재 새 leveler의 gain은 1입니다. 따라서 limiter는 작동하지만 요구한 독립 음량 증폭은 아닙니다. 코드에 명확한 TODO를 남겼고 실기검증에 stem별 VAD·시간 정렬·프레임별 독립 gain 구현 및 검사 계획을 추가했습니다. 모델·live 병합을 포함해 여전히 미구현이며 확률을 임의로 1로 넣지 않았습니다.

### 이번 검사와 다음 통과 기준

철회 회귀 검사 소스 5개를 추가해 XCTest는 23개입니다. **실행한 XCTest는 0개**입니다. 변경 후 정적 검사 8개 그룹, Swift 등 문법 파싱, 복사본 동기화 및 ZIP 내용 대조를 수행합니다. Worker/API 변경은 없으며 이전 계약 검사 결과와 이번 검사를 구분합니다.

현재 도구에서 접근 가능한 Apple SDK·Mac 호스트가 없으므로 최초 빌드·실행을 수행하지 못했습니다. 다음 통과 기준은 Phase 1을 실제 iPad Swift Playground에서 의존성 해결·컴파일하고, 오류를 수정한 뒤 일본어/영어 파일의 STT→실시간 번역→임시 결과 철회→단어/문장 팝업을 실행하는 것입니다. XCTest는 현재 연결된 Phase 2 Xcode target에서 별도로 실행해야 합니다. 실제 Apple 실행 전까지 사용 가능 판정을 내리지 않습니다.

## 이전 기록: 두 번째 추가 검토

2026-09-07. 앞선 재검토 수정본을 다시 읽고 비동기 시작/종료, 번역 실패 복구, 인증 갱신이 서로 연결되는 경로를 확인했습니다. **추가 오류를 수정했지만 빌드·iPad 실행까지 검증된 상태는 아닙니다.**

## 확인하고 수정한 내용

| 문제 | 영향 | 수정 |
|---|---|---|
| STT 시작 실패 후 정상 종료 메서드 호출 | 입력 sequence가 등록되지 않았다면 이후 sequence가 생기기를 기다리며 정리가 끝나지 않을 수 있음 | 실제 시작 성공 여부를 기록. 시작 실패는 `cancelAndFinishNow`, 시작 성공은 정상 finalize 경로 사용. 종료된 입력 큐에 추가할 때도 오류 반환 |
| Apple 번역 모델 준비 실패 후 재실행 경로 없음 | 이후 확정 자막이 계속 ‘번역 중’으로 남음 | 실패 상태를 행에 표시. 오류 때만 메뉴에 ‘번역 다시 연결’을 제공하고 같은 언어도 configuration invalidate로 재실행. 새 입력을 시작할 때도 준비 실패를 재시도 |
| 다른 언어의 번역 대기 항목을 전체 자막에서 반복 검색 | 이전 언어의 미번역 기록이 많을 때 현재 언어 대기 중 불필요한 검색 증가 | TranslationBacklog를 언어별로 분리. 취소된 작업은 해당 언어 대기열에 복원 |
| 언어 변경/번역 재시작 중 이전 비동기 응답 도착 | 이전 세션의 실패 상태나 결과가 새 상태를 덮을 수 있음 | 번역 작업에 언어와 revision을 고정하고 응답 적용 전 검사. 언어 변경 완료 전 새 입력 시작 차단 |
| WebView의 자체 갱신과 SwiftUI token 상태가 별도로 존재 | WebView가 새 token으로 갱신된 뒤 화면 업데이트가 이전 token을 다시 쓸 수 있음 | token 갱신은 LogsScreen의 취소 가능한 작업 하나가 담당. WebView는 그 현재 상태만 반영하고 페이지 로드 완료 때도 다시 적용 |
| 세션 resolve 요청 중 창 닫기/다른 세션 선택 | 늦은 응답이 닫힌 선택 창에서 저장을 진행하거나 중복 선택을 일으킬 수 있음 | 요청 작업을 보관해 창 종료 시 취소하고 응답 후 취소 검사. 요청 중 중복 선택도 제한 |
| 인증 발급을 함께 기다린 호출자의 설정 변경 검사 누락 | APP_TOKEN 변경 시 최초 호출자는 보호되지만 기다리던 다른 호출자는 이전 token을 받을 수 있음 | 공동 대기 경로에도 설정 ID 검사. API 요청 직전에 취소 여부 확인 |

STT 종료 분기는 [Apple의 finalizeAndFinishThroughEndOfInput 설명](https://developer.apple.com/documentation/speech/speechanalyzer/finalizeandfinishthroughendofinput())을 대조했습니다. 입력 sequence가 없으면 기다린다고 명시되어 있어 정상 종료와 시작 실패를 구분했습니다. 번역 재실행은 [TranslationSession.Configuration의 invalidate](https://developer.apple.com/documentation/translation/translationsession/configuration)를 사용합니다.

## 검사와 한계

- Swift 31개(복사본·Package.swift·XCTest 포함), Rust 1개, Python 1개, shell 2개 문법 파싱: 오류 노드 0.
- 소스/리소스 참조, target 포함, scheme, capability, 복사본 동기화, 일본어 데이터 구조 등의 정적 검사 8개 그룹 통과.
- 제공 Worker의 격리 계약 검사 10개 재실행. 실제 서비스 호출/운영 DB 저장 없이 검사.
- 언어별 번역 대기열과 취소 후 복원에 대한 회귀 검사를 추가해 XCTest 소스는 총 18개. **Apple 환경이 없어 XCTest 실행은 하지 못했으며, 성공으로 집계하지 않습니다.**
- 최종 ZIP과 실제 폴더의 파일 목록 및 모든 파일 내용을 SHA-256으로 대조합니다. 결과는 ZIP 옆의 `archive-verification.json`에 기록합니다.

이번 수정에서 추가한 오류 복구 경로도 실제 iPad에서 재현 검사가 남아 있습니다. 특히 시작 실패 후 재시작, 번역 모델 다운로드 거부 후 재연결, 언어 빠른 전환, 장시간 단어장 token 갱신, 세션 선택 취소를 실기로 확인해야 합니다. 문법 파싱을 Swift 타입 검사나 동작 테스트로 대체하지 않습니다.

## 완료되지 않은 범위

Apple SDK 빌드·패키지 resolve·iPad 실행·전력/발열 검증, Sudachi 사전/바이너리 연결, 실제 2화자 모델 및 live 자막 병합은 여전히 남아 있습니다. 이 추가 검토는 그 항목들을 구현 완료로 바꾸지 않습니다. 기존 Worker·웹·GCP를 수정하거나 배포하지 않았습니다.
