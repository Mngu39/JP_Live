# 실행한 검사 기록

> **최신 상태: [현재상태.md](현재상태.md), audio-timeline-2026-09-10.** 이전 사본은 사용자 iPad에서 컴파일·앱 실행·STT 준비에 성공했으나 스트리밍 timestamp 오류로 실패했습니다. 이번 수정본의 Apple 빌드·72개 XCTest 실행·iPad runtime은 미실행입니다. 아래 이전 검토의 수치/판정은 해당 시점의 기록입니다.

2026-09-09 전달본 정리, Windows, Node.js 24.18.0. Apple SDK는 이 환경에 없습니다. 사용자 보고상 의존성 제거본은 resolve를 통과한 뒤 LanguageTools의 compiler 오류 3건에서 멈췄습니다. 이번 기록은 세 오류의 수정과 JSON 로드 확인 표시의 정적 검사이며, 수정본 Apple compile/run과 구분합니다.

| 검사 | 결과 | 의미/한계 |
|---|---|---|
| 이번 변경 Swift 5개 문법 파싱 | 2026-09-08 오류 노드 0 | LanguageTools/Settings와 복사본, XCTest. Swift 타입/Apple API 검증 아님 |
| JSON 구조·복사본·Phase 2 리소스 target | 통과 | 원본 한자 5663·deck 인덱스 2306개. 실제 앱 bundle 로드 아님 |
| 변경 범위 대조 | 2026-09-09 통과 | 이전 ZIP과 비교해 위 Swift 5개만 변경. Phase 1 manifest·Phase 2 프로젝트·리소스 원본 동일 |
| 이전 전체 문법 및 자동 검사 13개 그룹 | 2026-09-08 통과 기록 유지 | 이번에 전수 재실행하지 않음 |
| Phase 1 외부 SPM/remote binary/build plugin | 선언 0개 | package/product 모두 제거, 오래된 Package.resolved/빌드 캐시 미포함 |
| Phase 2 FluidAudio/DeepFilter 고정 커밋 | 유지 확인 | 전이 버전 resolve 및 실제 Package.resolved 생성은 미실행 |
| 제공 Worker 격리 계약 | 이전 10/10 통과 | 가짜 DB·DeepL·Groq 응답 사용. 이번 재실행/운영 호출 없음 |
| Playgrounds 소스 동기화 | Shared 16개 byte 일치 | 이번 패키징 전에 SHA-256 대조 |
| Swift XCTest 55개 | 미실행 | 앱 bundle의 두 JSON decode 검사 1개 추가. 실제 실행 0개 |
| 이전판 사용자 iPad 결과 | 의존성 통과 후 compiler 오류 3건 보고 | CFLocaleIdentifier·StringTransform 추론·Bundle.module. 그 이전 Nemo/unzip 실패와 구분 |
| 이번 LanguageTools 수정본 Apple compile/run·JSON 로드 | 미실행 | 다음 실제 iPad 검증 대상 |
| Xcode resolve·빌드·XCTest·iPad 성능 | 미실행 | 실제 lockfile/IPA 없음. 실기검증.md 절차 유지 |

## 이번 LanguageTools 수정 및 리소스 확인 검사

정식 locale 식별자 생성→CFLocaleCreate 전달, 명시적인 StringTransform 타입, LearningData의 Bundle.main 기반 조회와 설정의 실제 decode 상태 연결을 확인했습니다. Swift 5개 변경 파일의 tree-sitter 문법 검사와 모든 Shared 16개 복사본 대조는 통과했습니다. 두 JSON의 문자열 사전 구조, 원본 건수, Playground 복사본, Xcode 앱 리소스 포함도 확인했습니다. 결과는 `Tests/static-review-results.json`의 `languageToolsRevision`에 기록했습니다.

작업 중단 뒤 재개하면서 이전 ZIP과 소스를 비교했습니다. 변경된 Swift는 LanguageTools/Settings와 각각의 복사본 및 XCTest뿐이며, Package.swift·Xcode 프로젝트·원본 리소스는 바뀌지 않았습니다. 실제 컴파일/API 가용성이나 Bundle.main의 기기 리소스 위치를 Windows에서 검증했다고 표현하지 않습니다.

앱 실행 후 **… → 설정 · 연결 상태 → 학습 데이터 확인**에서 한자 사전 5663개·단어장 인덱스 2306개 로드를 확인해야 합니다. 이 수치는 동봉 원본의 기대값이며 아직 실제 iPad 성공 기록이 아닙니다. 실패하면 파일 없음/읽기 실패/형식 오류/빈 데이터가 표시됩니다. hosted XCTest의 두 JSON decode 검사도 실행 전입니다.

## 이전: 의존성/선택 모델 준비 수정 검사

13개 자동 검사 중 추가한 5개는 Phase 1의 비어 있는 의존성과 binary/plugin/cache 부재, Phase 2 커밋·제품 및 전이 의존성 기록, 첫 기본 입력 이후 준비 시작과 완료 콜백 격리, 늦은 분석/개선의 시간 경계, 실제 Package.resolved 보존 절차입니다. 기존 검사는 전체 문법 트리·Xcode 참조·target·scheme·리소스·복사본·스크립트 줄바꿈을 확인합니다. 실행 옵션 `--no-opt --liftoff-only`에서 종료 코드 0을 확인했습니다.

회귀 XCTest 4개는 준비 중에도 기본 PCM이 나오는 경로와 완료 전 중지, 늦은 분석이 새 PCM 원점부터 적용되는 경로, 늦은 enhancer가 이전 분석 대기 PCM을 처리하지 않는 경로, enhancer가 먼저 준비되는 순서를 다룹니다. 테스트용 지연 provider는 XCTest target에만 있고 Phase 1에 가짜 분석기를 넣지 않았습니다. XCTest는 실제 실행 전입니다.

`Tests/static-review-results.json`의 `dependencyRevision`이 이번 의존성 변경 기록입니다. 이전 `prebuildRevision`/`previousPrebuildRevision`은 gain/soft gutter 수정 때의 검사로 보존합니다. 실제 모델 다운로드 취소, iPad의 기본 입력 시작/표시 지연, Apple 자산 설치와 새 컴파일 오류는 현재 환경에서 확인할 수 없습니다.

## 이전: 단일 화자 gain 수정 검사

알려진 단일 화자 A→B에서 gain 초기화, unknown/낮은 확률 slot의 초기화·ID 교체 방지에 한정해 Audio/AudioPipeline과 XCTest를 변경했습니다. 복사본 포함 Swift 5개의 문법 파싱, 기존 증폭 gate 및 새 slot 전달/초기화의 소스 연결 검사, 전체 Shared 16개·리소스 복사본 대조를 수행했습니다. 소스 연결 검사는 Swift 동작 실행을 대신하지 않습니다. 실제 최초 compile/run 및 XCTest는 미실행이며 전체 ZIP/Playground ZIP의 파일별 내용 대조 기록은 `archive-verification.json`에 있습니다.

회귀 XCTest 2개는 enhancer 없는/지연 출력 경로의 quiet A→loud B 첫 frame, unknown/낮은 확률 ID 유지 조건을 다룹니다. `Tests/static-review-results.json`의 `prebuildRevision`이 이번 기록이고 `previousPrebuildRevision`은 직전 수정 검사입니다. Node에는 기존 `--no-opt --liftoff-only` 옵션을 사용합니다.

## 이전: overlap 증폭 및 soft gutter 검사

오디오 증폭 허용 조건과 soft gutter mapping에 관련된 6개 Swift 소스 및 복사본, XCTest만 tree-sitter로 검사했습니다. raw slot modulo 색 선택 제거, 별도 gutter hint 연결, 단일 화자 증폭 gate의 소스 존재도 확인했습니다. 이 소스 확인은 실행 검사를 대신하지 않습니다. 전체 Shared 16개 복사본과 리소스가 기준본과 같고, 새 ZIP의 각 파일이 폴더의 최신 바이트와 같은지도 대조했습니다. `Tests/static-review-results.json`의 `prebuildRevision`에 이번 범위를 별도로 기록합니다.

추가 XCTest 9개는 단일→겹침/무음 전환에서 잔여 gain 제거, limiter 유지, 겹침 2/3명에서 DF 호출 우회, 단독·slot 0/2·제3/unknown·캐시 만료·중복 시간·입력 초기화를 다룹니다. 모두 Apple 환경에서 실행해야 합니다. Node 실행 옵션은 `--no-opt --liftoff-only`이며, 기존 검사 환경의 V8 문제를 피하기 위한 것으로 앱 코드를 변경하는 옵션이 아닙니다.

## Worker 계약 10개

1. 인증 없는 token 발급 거부: handler rejection 확인.
2. 기존 APP_TOKEN으로 24시간 log token 발급.
3. 일본어 lemma를 기존 DeepL JA 경로에 전달.
4. 영어 팝업을 DeepL EN 경로에 전달.
5. 이미지/bbox 없는 문장 저장 허용.
6. 이모지 뒤 단어의 UTF-16 범위와 명시적 context group 보존.
7. target_word 없는 단어 저장 거부: handler rejection 확인.
8. 변조된 token 거부: handler rejection 확인.
9. 만료된 token 거부: handler rejection 확인.
10. 기존 AI 재구성 endpoint의 UTF-16 범위 및 원문 결합 보존.

거부 검사 통과는 dispatcher의 올바른 HTTP 오류 응답을 뜻하지 않습니다. 해당 문제는 검토보고서에 별도 기록했습니다. Worker 검사 재실행에는 제공받은 원본 worker/src/index.js 경로를 `Tests/worker-contract.cjs`의 인자로 전달합니다. 원본 Worker나 사용자 인증 정보는 배포 묶음에 넣지 않았습니다.

## 추가 정적 검사에서 수정한 사항

- DeepFilterNet의 짧은 초기 출력과 종료 tail에 맞춰 시각을 유지하고 tail에도 limiter를 적용했습니다.
- 화자 분석 오류/큐 초과와 음성 개선 오류의 fallback 상태를 설정에 표시했습니다.
- UTF-16 범위 검사와 source 보존을 저장/AI 전환에 적용했습니다.
- 최종 source와 Playgrounds 복사본을 다시 동기화했습니다.

이 조치의 소스 존재와 문법 파싱을 확인했으며, 음성 출력/화면 품질을 실기에서 관찰한 결과는 아닙니다.

## 이전: 외부 지적 1~4 반영 후 검사 범위

`AudioPipeline.swift`와 `SpeechTimeline.swift`를 추가하고 Xcode app target 및 Playground 복사본에 연결했습니다. 오디오 구간 정렬, DF 추론 우회, 번역 retry/backlog, 발화 중 tick 분할, 형태소 비동기 연결 변경에 대해 동일 정적 검사 8개 그룹을 수행했습니다. 최종 Swift 파일 수는 35개입니다. 실행 옵션은 Node `--no-opt --liftoff-only`이며 종료 코드 0을 확인했습니다. Worker 계약 10/10은 이전 실행 기록이며 이번에는 운영 API를 호출하지 않았습니다.

XCTest 소스는 39개입니다. 새 시나리오는 delayed analysis와 원본 시각 대응, unknown/화자 교체 구간, 침묵의 시간 공백, DF 처리 호출 횟수 및 우회 tail 보존, 분석 대기 상한과 짧은 파일 EOF, 자동 재시도 상한·간격·다른 행 진행·수동 재시도·취소, volatile 연속 갱신·실제 발화·접속형·최대 길이를 다룹니다. Apple 환경이 없어 **이 검사들을 실제 실행한 수는 0개**입니다.

전체 ZIP과 Playground ZIP의 최신 파일 수·내용 해시는 ZIP 옆 `archive-verification.json`을 확인합니다. 다음 통과 기준은 Phase 1의 실제 Apple SDK 컴파일과 일본어/영어 오디오 실행입니다.

## 이전 기록: 빈 결과 철회 및 gutter 지적 반영

빈 결과 철회 및 gutter 소스 변경 후 동일 정적 검사 8개 그룹을 재실행하고, Shared/Playground 복사본과 두 ZIP을 다시 동기화·대조했습니다. Swift 파일 수는 31개로 동일합니다. 문법 파서는 tree-sitter이며 `swiftc -parse`나 Swift 타입 검사 실행으로 표현하지 않습니다. Worker 코드는 이번에 변경하지 않았으며 위 10/10은 앞서 수행한 격리 검사 기록입니다.

검사 도구의 기본 Node 실행에서는 결과를 출력한 뒤 V8의 `Fatal process out of memory: Zone`으로 비정상 종료했습니다. 실행 옵션 변경에도 반복되어, 최종적으로 Node에 `--no-opt --liftoff-only`를 지정하고 같은 검사 코드를 재실행했습니다. 이 실행은 모든 검사 통과 및 종료 코드 0을 확인했습니다. 해당 옵션은 Windows 검사 도구에만 적용하며 Swift 앱이나 검사 항목을 변경한 것이 아닙니다.

새 회귀 검사 5개는 임시 행 전체 철회(빈 final 포함), 확정 prefix의 ID·시각 보존, 확정 이력 보존, 다른 구간·잘못된 시각 무시, 철회 뒤 새 문맥 생성을 다룹니다. XCTest 실행과 긴 행 gutter의 실제 위·중간·아래 탭 검증은 Apple 환경에서 수행해야 합니다.
