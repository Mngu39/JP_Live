# 중단 이력 이후 재검토

> **최신 상태: [현재상태.md](현재상태.md), audio-timeline-2026-09-10.** 이전 사본은 사용자 iPad에서 컴파일·앱 실행·STT 준비에 성공했으나 스트리밍 timestamp 오류로 실패했습니다. 이번 수정본의 Apple 빌드·72개 XCTest 실행·iPad runtime은 미실행입니다. 아래 이전 검토의 수치/판정은 해당 시점의 기록입니다.

2026-09-07. **압축 파일 누락은 없었지만, 이전 문법 검사로 발견하지 못한 동작상 문제들이 있었습니다.** 아래 문제를 수정했고 두 압축본을 다시 만들었습니다. 아직 Apple SDK 빌드·iPad 실행 성공을 확인한 앱은 아닙니다.

## 중단 때문에 파일이 유실되었는가

수정 전 기존 ZIP을 풀지 않고 모든 항목의 SHA-256을 폴더의 실제 파일과 비교했습니다.

| 묶음 | ZIP 파일 수 | 폴더 파일 수 | 내용 불일치 |
|---|---:|---:|---:|
| 기존 전체 프로젝트 | 53 | 53 | 0 |
| 기존 Playgrounds | 17 | 17 | 0 |

이는 당시 압축본과 폴더가 일치했다는 증거입니다. 구현이 완성되었다는 증거는 아닙니다. 실제 문제는 누락보다 상태 처리와 오류 복구에 있었습니다.

## 발견하고 수정한 문제

| 중요도 | 이전 문제와 영향 | 이번 수정 |
|---|---|---|
| 높음 | 모델 준비 작업은 runTask 밖에 있어 중지 후에도 이전 준비가 계속되어 상태를 바꿀 수 있었음 | 준비부터 입력 처리·종료까지 하나의 소유 작업으로 관리. 중지는 취소 및 정리 완료를 기다림. 새 입력은 정리 완료 뒤 허용 |
| 높음 | STT partial→final에서 행을 삭제하고 새 UUID로 생성. 아직 chunk로 확정되지 않은 final prefix도 잠시 화면에서 사라짐 | TranscriptBuffer로 결과 상태를 분리. 첫 chunk는 기존 행 ID 유지, pending prefix도 계속 표시. 확정 prefix를 내보낼 때 수정 중 tail 보존 |
| 높음 | 수정 중 원문을 저장하면 나중 확정된 문장과 텍스트/그룹이 달라질 수 있었음 | 번역 조회는 허용하고 저장은 확정된 자막에서만 허용. 팝업과 저장 payload 양쪽에서 검사하며 화면에 이유 표시 |
| 높음 | 화자 모델의 과거 시각을 최근 입력 시각으로 덮어써 오래된 판단으로 gain/화자 표시를 정할 수 있었음 | 모델 시각을 유지하고 새 결과가 없으면 이전 결과를 새 결과처럼 반환하지 않음. 오래되거나 미래인 판단 제외 |
| 높음 | 음성 개선 모델 오류 때 내부 dry 대기분이 버려질 수 있었음 | 아직 출력하지 않은 dry samples 회수 후 limiter를 거쳐 기본 STT로 전달. 종료 flush 오류에도 회수 |
| 높음 | 처리 출력 시각을 계속 이어 붙여 입력 공백이 사라질 수 있었음 | 원본 시각 유지. 구간이 불연속이면 이전 tail을 먼저 내보내고 분석/개선 상태를 초기화한 뒤 새 구간 처리 |
| 높음 | 캡처 picker의 재선택과 중지가 겹치면 늦게 시작된 stream이나 이전 stream 오류가 남을 수 있었음 | 선택 요청 ID와 active 상태 검사, 이전 stream의 출력/오류 무시, 중지 후 시작 완료된 stream도 다시 종료 |
| 중간 | 단어 직접 탭에도 문장 전체 DeepL 호출이 발생 | 단어 진입은 단어만 요청. 문장을 열거나 AI 재구성/문맥 저장에 필요할 때 문장 요청 |
| 중간 | 영어 단어의 기본형 표시 중 일본어 reading 함수를 호출할 수 있었음 | reading 계산을 일본어 분기로 제한. 일본어 기본형 읽기는 현재 연결된 tokenizer를 재사용 |
| 중간 | CoreText line origin을 view 절대좌표로 사용해 탭 영역이 inset만큼 어긋남 | frame path 원점을 더해 실제 글자 좌표와 맞춤 |
| 중간 | token 갱신 중 APP_TOKEN을 바꾸면 이전 요청이 새 인증 캐시를 덮을 수 있었음 | 인증 설정 세대 ID 확인과 취소 검사 |
| 중간 | WebView token 갱신 주기가 1시간이어서 만료 후 갱신까지 공백 발생 가능 | 1분마다 캐시 만료 확인, 앱 활성화 시에도 갱신. 평소에는 캐시를 사용하므로 매분 서버 발급을 요청하지 않음 |
| 중간 | 저장 요청을 기다리는 동안 선택 단어/AI 모드를 바꾸면 대상이 바뀔 수 있었음 | 저장 시작 시 token·형태소·번역 상태를 고정 |
| 중간 | 반복 IPA 패키징 시 이전 Payload 안의 오래된 파일이 섞일 수 있었음 | 매 빌드 새 작업 폴더 사용, 성공한 새 IPA만 결과 위치로 이동 |
| 중간 | STT 실패를 상태 문자만 바꾸거나 실험 경로에서 무시 | 후속 입력에서 실패를 반환하고 실험 경로도 오류를 전달. 종료 실패 시 결과 작업을 취소해 대기 정리 |

CoreText 좌표 수정은 [Apple의 line origin 설명](https://developer.apple.com/documentation/coretext/ctframegetlineorigins(_:_:_:))에 근거합니다. STT 결과가 같은 구간에서 수정되고 확정되는 동작은 [SpeechTranscriber.Result 문서](https://developer.apple.com/documentation/speech/speechtranscriber/result)를 대조했습니다.

## 이번에 실제로 실행한 검사

- Swift 29개(원본·Playgrounds 복사본·manifest·XCTest 포함), Rust 1개, Python 1개, shell 2개의 문법 파싱: 오류 노드 0.
- Xcode가 참조하는 소스/리소스 존재, Shared 소스의 target 포함, scheme target ID, Info.plist, iOS 테스트 host 경로, 복사본 일치, 일본어 JSON 구조, shell 줄바꿈 등 8개 정적 검사 그룹 통과.
- 한자 DB 5,663개와 deck 인덱스 2,306개가 Swift에서 기대하는 문자열 map 구조인지 확인.
- 첨부 Worker 격리 계약 검사 10/10 통과. 실제 DeepL/Groq/운영 DB 호출 0회.
- 최종 압축본을 다시 열어 모든 파일 내용과 파일 수를 확인하고 SHA-256 목록 갱신.

기계 판독 결과는 `Tests/static-review-results.json`, Worker 실행 기록은 `Tests/worker-contract-results.txt`에 있습니다.

XCTest는 기존 7개에서 **16개**로 늘렸습니다. 추가 항목은 행 ID 유지, final prefix/tail 보존, 새 run의 context 분리, 미확정/영어 저장 거부, 영어 공백, 미확정 종료 표시, stale 화자 판단, DF 오류 dry 회수, 입력 시각 공백입니다. **XCTest 소스는 작성하고 문법 검사했지만 실행하지 못했습니다.** 실제 Swift 코드의 타입 검사나 이 테스트들의 성공을 주장하지 않습니다.

## 여전히 완료 판정을 못 하는 부분

1. Windows에는 해당 Apple SDK가 없어 Playgrounds/Xcode 빌드·링크를 수행하지 못했습니다. 패키지 의존성의 실제 resolve/컴파일 호환성과 네이티브 동시성 검사는 남아 있습니다.
2. YouTube 시스템 PCM 수신, Windowed Apps, ruby/탭의 실제 화면, STT/번역, 전력·발열은 iPad 측정 전입니다.
3. Sudachi 사전과 iOS 바이너리가 아직 연결되지 않았습니다. Apple fallback의 형태소/읽기를 Sudachi 수준으로 보증하지 않습니다.
4. 2화자 분리는 여전히 모델 변환 실험 및 독립 실행 소스 단계입니다. 실제 모델과 live 구간 추출·stem leveling·자막 대체/병합은 완성되지 않았습니다.
5. 아주 긴 시청에서 transcript 전체 메모리 증가, 화자 판단의 정확도, 전처리가 STT에 주는 실제 개선 정도는 확인되지 않았습니다.

기존 Worker의 async 오류 응답 문제는 검토보고서에 유지했습니다. 원본 Worker·웹·GCP를 수정하거나 배포하지 않았습니다. 이번 수정은 새 프로토타입 파일 안에서만 이루어졌습니다.

**결론: 중단으로 깨진 ZIP은 아니었고, 실제 코드 문제를 찾아 보완했습니다. 그래도 현재 정확한 명칭은 ‘수정된 빌드 전 소스 프로토타입’입니다.**
