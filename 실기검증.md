# 최초 빌드 및 iPad 검증

현재 기준은 [현재상태.md](현재상태.md), **audio-timeline-2026-09-10**이다. 이전 사본의 dependency resolve·Swift 컴파일·앱 실행·파일 입력·STT 준비는 사용자 보고상 통과했다. STT 스트리밍은 `Audio input timestamp overlaps or precedes prior audio input`에서 실패했다.

이번 시간축 수정본은 Windows 소스 검사를 마쳤으나 **Apple compile/XCTest/iPad runtime은 미실행**이다. iPad에서 오류 하나씩 찾아 달라고 요청하기 전에 `Tools/test-apple.sh 1`로 같은 Phase 1 소스를 Apple compiler·72개 XCTest·기기용 build에 통과시키는 것을 다음 관문으로 삼는다. 현재 연결된 Mac 호스트와 실행 승인된 앱 전용 GitHub 저장소가 없다. 자동 빌드 파일 작성과 CI 실제 실행은 별개다.

아래 목록은 이후 실행 묶음의 acceptance 항목이며, 이미 실행했다는 기록이 아니다. Phase 1 Xcode test host는 Playground의 포장 환경을 대신하지 못하고, simulator XCTest는 기기의 STT/Translation 모델 성공을 보증하지 못한다.

## 1. 빌드 관문

1. Apple compiler/XCTest 관문을 통과한 뒤, Phase 1 ZIP을 새 폴더에 풀어 그 `JPLive.swiftpm`을 Swift Playground에서 열고 dependency 확인→컴파일→실행한다. 외부 package/product가 없으며 Nemo 다운로드가 반복되면 이전 사본인지 확인한다. 첫 실패의 오류 원문과 OS/Playgrounds/SDK/Swift 버전을 기록한다.
2. Phase 2 프로젝트를 Xcode에서 resolve하고 시뮬레이터 build/test를 수행한다. 생성한 `Phase2/JPLive.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved`를 프로젝트와 함께 보존한다. FluidAudio/Nemo 및 DeepFilterNetMLX/MLX/HuggingFace를 포함한 실제 그래프를 확인하고, archive는 이 버전을 고정해 사용한다. 아직 파일이 없는 상태를 고정 완료로 취급하지 않는다.
3. Swift 타입/격리 오류, 링크 오류, 패키지 최소 버전 오류를 해결한 뒤 Release 기기 archive를 만든다. 문법 파서 통과를 이 결과로 대체하지 않는다.
4. 서명/설치 후 실제 기기에서 실행한다. Apple STT·번역 모델을 내려받고 일본어/영어 파일 각각으로 자막을 확인한다. 모델 준비 실패와 입력 형식 오류도 확인한다.

앱 화면에 들어가면 **… → 설정 · 연결 상태 → 학습 데이터 확인**에서 먼저 JSON 로드를 확인한다. 동봉 원본 기준 한자 사전 5663개·단어장 인덱스 2306개가 표시되어야 한다. 실패 메시지이면 이를 원문 그대로 기록하고 리소스 포함/실제 bundle 위치를 확인한다. `LearningData`의 decode 결과를 쓰는 표시이므로 파일만 발견한 상태를 성공으로 처리하지 않는다. 일본어 단어 팝업에서도 실제 한자 정보와 기존 deck 설명이 나오는지 이어서 확인한다. Mac의 hosted XCTest에도 앱 main bundle의 두 JSON이 비어 있지 않게 해석되는 검사가 있으나 실행 전이다.

Phase 1에서 화자 분석은 비활성이다. 기본 STT·번역·팝업·일본어 저장 검증과 Phase 2의 diarization/화자별 증폭/화자 전환 분할/soft gutter 검증을 구분한다. Phase 1에서는 gutter 중립색, 음성 확률 없는 limiter 경로, 화자 ID 없는 chunk 경계가 예상 동작이다.

## 1a. Phase 2 선택 모델 준비와 시작/중지

- Apple STT 모델은 준비된 상태에서 FluidAudio/DeepFilter 모델 캐시가 없는 첫 실행을 비교한다. picker와 첫 기본 STT PCM이 선택 모델 다운로드 시작보다 먼저여야 한다. 필수 Apple 음성 모델 준비와 선택 모델 준비 시간을 분리해서 기록한다.
- 선택 모델 네트워크를 지연/실패시켜도 원본 기반 STT가 계속돼야 한다. 화자 분석과 enhancer는 각각 준비 상태/실패를 표시해야 한다.
- 분석이 늦게 준비되면 다음 PCM의 실제 시각을 원점으로 삼고 이전 PCM을 재전송하지 않아야 한다. 연결 경계 전후의 샘플 수·시각·중복/누락을 검사한다.
- 분석이 이미 오디오를 보관하는 중 enhancer를 연결한다. 설치 전 입력은 원래 경로, 이후 입력은 확인된 단일 화자에서만 enhancement를 사용해야 한다. enhancer가 분석보다 먼저 준비되는 순서도 검사한다.
- 다운로드 중 중지→재시작/언어 변경/짧은 파일 EOF를 수행한다. 중지가 선택 모델 준비 완료를 기다리지 않아야 하고 늦은 이전 결과가 새 capture에 붙으면 안 된다. 취소된 다운로드의 실제 중단·정리 속도와 반복 재시작 시 메모리도 계측한다.

## 2. 핵심 목적: YouTube PCM

1. 시스템 오디오 시작 → 전체 화면 공유를 선택하고 YouTube 공식 앱에서 일반 영상을 재생한다. 마이크로 받은 소리로 대체하지 않는다.
2. 자막이 생성되고 입력 RMS가 움직이는지 확인한다. 앱이 양쪽 창으로 보일 때, 다른 창에 초점이 있을 때, 창 크기를 바꿀 때 각각 검사한다.
3. 공유 중단/취소와 YouTube 영상 전환을 수행한다. 오류가 사용자에게 보이고 다시 시작할 수 있어야 한다.
4. 같은 영상의 같은 20초 구간을 시스템 볼륨 10/50/100%로 각각 재생한다. RMS/peak를 동일 구간에서 기록한다. 귀로 들리는 크기만으로 PCM 독립성을 판단하지 않는다.

| 볼륨 | 입력 RMS 평균 | peak | STT 결과 | 판정 |
|---|---|---|---|---|
| 10% | 미측정 | 미측정 | 미확인 | 미검증 |
| 50% | 미측정 | 미측정 | 미확인 | 미검증 |
| 100% | 미측정 | 미측정 | 미확인 | 미검증 |

**전체 input normalization은 미구현이다.** 위 측정과 Phase 1의 동일 음원 원본/저레벨 파일 비교를 바탕으로 diarization과 독립된 앞단 입력 보정을 구현한다. 모델 준비 실패·timeout·미확정에서도 저레벨 입력의 RMS/peak와 인식 결과를 비교한다. 무음·BGM·효과음의 과증폭을 피하고, 충분히 큰 입력에는 불필요한 증폭을 하지 않도록 상한·응답 시간을 정한다. 현재 설정의 수치를 읽는 것만으로 보정 완료라고 판정하지 않는다.

## 3. UI·번역·학습

- 개별 행의 Apple 번역에 일시 오류를 유도한다. 1초·3초 뒤 최대 2회 재시도하며 대기 중에도 다른 행의 번역은 진행되어야 한다. 세 번째 실패 뒤 자동 호출은 멈추고 ‘실패한 번역 재시도’ 메뉴로 다시 요청할 수 있어야 한다. 언어 전환/취소를 실패 횟수로 소비하지 않아야 한다.
- volatile 문장이 계속 갱신되고 실제 발화가 이어질 때 final 수신 후 1.2초만으로 문장이 끊기지 않아야 한다. 분석이 없으면 volatile 없는 STT idle 경로를 사용한다. 실제 침묵·접속형과 최대 9초 제한에 의한 분할을 서로 구분해 확인한다.
- 후리가나·단어 분석이 늦는 경우에도 원문 스크롤이 반응해야 한다. 분석 중 gutter로 연 팝업에도 해당 snapshot의 단어 정보가 나중에 나타나야 한다. 철회되거나 수정된 이전 원문의 token 결과가 새 자막에 적용되지 않아야 한다.
- 번역 모델 준비를 실패/취소한 후 메뉴의 ‘번역 다시 연결’로 재실행한다. 실패 중 원문은 계속 표시되고 오류 문구가 ‘번역 중’과 구별되어야 한다.
- 모델 준비 중 중지하고 다시 시작한다. 언어 전환을 빠르게 반복해 이전 작업이 새 언어·상태를 덮지 않는지 확인한다.
- 세션명/URL 확인 요청 직후 선택 창을 닫는다. 늦은 응답이 저장을 실행하지 않아야 한다.

- 같은 자막 ID/history를 유지하며 Rail→Dock→큰 창을 반복한다. 원문/번역 행이 어긋나지 않아야 한다.
- dark/light, 긴 일본어·영어, 이모지/결합문자, 글자 크기 증가에서 ruby와 탭 위치를 확인한다.
- 여러 줄 caption의 gutter 위·중간·아래를 각각 눌러 같은 문장 팝업이 열리는지 Rail/Dock에서 확인한다. 오른쪽 본문 단어 탭은 계속 별도로 작동해야 한다.
- 단독 화자 영상은 중립 gutter를 유지해야 한다. 확인된 최근 A↔B(내부 slot 0/2 포함)는 서로 다른 보조 색을 사용하고, 단발성 NPC/제3 화자·unknown·임시 STT는 중립이어야 한다. 짧은 보조 캐시 만료 후 단독 발화와 새 입력 시작 시 중립으로 돌아가야 한다. 표시 기준은 현재 각 화자 2회/누적 1.2초, 보조 후보 3초/확인된 보조 6초/주 화자 12초이며 실제 A→B→A 자료로 조정한다. 과거 행의 표시나 내부 chunk 경계가 UI 캐시 때문에 바뀌지 않아야 한다.
- 임시 자막 뒤 같은 구간의 빈 volatile 또는 빈 final 결과가 오면 임시 텍스트가 사라져야 한다. 앞서 확정된 prefix·번역 이력은 유지되어야 한다. 실제 framework 결과를 관찰하고 XCTest의 철회 시나리오도 실행한다.
- 일본어·영어에서 각각 gutter→문장, 단어→lemma 번역, 같은 팝업 뒤로가기를 확인한다.
- 첫 빌드 후 영어 단어 문맥 번역을 구현한다. 현재는 lemma만 전송하므로 이 항목은 아직 통과 불가다. Worker 변경이 필요하면 계약·호환성 변경을 먼저 보고한다. 팝업을 연 caption snapshot으로 문맥을 고정하고 run/charge/issue를 다른 문장에서 눌렀을 때 의도한 뜻인지 비교한다. 뒤에 도착한 STT 행으로 문맥이 바뀌면 안 된다.
- 일본어 AI 재구성 전후 원문 보존과 token 선택 대응을 확인한다. 영어에는 일본어 학습/저장 기능이 없어야 한다.
- 실제 운영 저장은 사용자가 고른 테스트 세션에서만 수행한다. 같은 chunk의 문장과 두 단어가 같은 group이고 다른 run의 동일 문장은 다른 group인지 확인한다.
- 단어장 WebView에서 저장 결과를 확인한다. APP_TOKEN이 URL이나 웹 저장소에 전달되지 않아야 한다.
- 만료 전 갱신과 401 재발급을 테스트한다. 첨부 Worker의 async 응답 오류는 검토보고서 참조. 네트워크 저장 오류를 자동 재전송해 중복 저장하지 않아야 한다.

## 4. 전처리·분리 개발 검사

깨끗한 음성, 낮은 목소리, BGM만, 게임 효과음, 큰/작은 화자 교대, 두 명 겹침, 세 명 겹침, 역위상 stereo를 포함한 고정 자료로 개발자가 비교한다. 각 단계의 시간축, 입력·출력 peak, STT 오류를 기록한다. 과증폭·clipping과 실제 인식 악화 여부를 함께 본다.

마지막 pre-build 회귀 검사에서 낮은 단일 화자 입력으로 gain을 올린 뒤 높은 음성 확률의 2명/3명 겹침으로 전환한다. mixed 구간 첫 샘플부터 잔여 화자 증폭 없이 출력하고, 그 구간에서 DF 추론을 호출하지 않아야 한다. unknown/무음 전환에서도 잔여 gain이 없어야 한다. 큰 mixed 입력은 limiter 상한을 지키고 구간 경계의 샘플 수·시각은 보존해야 한다. 이 검증은 전체 input normalization 완료를 뜻하지 않는다.

추가된 단일 화자 교체 검사에서는 quiet A→loud B를 모두 알려진 single-speaker로 입력한다. B의 첫 샘플부터 A의 증폭을 상속하지 않고 B 자신의 레벨에서 시작해야 한다. enhancer 사용/우회 양쪽에서 B의 원래 시각과 전체 샘플 수를 보존하는지 확인한다. 단일 화자 판단을 유지한 채 A→slot nil→A, A→낮은 확률의 B→A도 입력해 ID 불확실성만으로 화자 교체 초기화를 하지 않는지 확인한다. 이 새 조건과 기존 non-single 구간의 증폭 차단을 구분해 검사한다.

Sudachi 바이너리/사전 연결 뒤 활용형 lemma, 이름, 숫자, 이모지가 있는 문장에서 token 범위와 reading을 검증한다.

separator는 아직 live 미연결이다. 변환 성공·모델 출력 일치·실제 두 음성 분리 품질을 먼저 검증하고 다음을 구현한 뒤 검사한다: overlap 구간 추출, stem 독립 leveling, 두 STT 병행, 창 사이 stem 대응, mixed 결과와의 중복 제거, 실패 시 기존 자막 유지. 3명 이상을 분리 경로에 넣지 않는다.

stem leveling은 각 stem을 짧은 프레임으로 나눠 독립 음성 판정 확률과 독립 gain 상태를 적용해야 한다. 현재 실험의 `speechProbability: nil`은 증폭을 하지 않는다. 이를 임의로 1로 바꾸어 음성 검출 없이 증폭하는 방식으로 완료 처리하지 않는다. 낮은 음성은 보정하고 무음/분리 잔여 소음은 증폭하지 않는지 입력·출력 RMS/peak와 인식 결과를 함께 확인한다.

화자 전환 chunk 경계는 오디오 타임스탬프에 맞춘 화자 이력을 연결한 다음 구현한다. 알려진 단일 화자 A→B에서는 확정 pending을 구분하고, unknown/겹침/오래된 판단만으로는 강제 분할하지 않아야 한다. STT 한 결과 안에서 화자가 바뀌는 경우에는 단어별 오디오 범위까지 연결해야 하며 현재 이 경로는 미완료다.

최신 수정에서 구간별 화자 이력과 final 결과 사이의 확인된 화자 전환 분할을 연결했다. 위의 한 STT 결과 내부 단어 단위 분할은 여전히 미완료다. 실제 LS-EEND 프레임의 start/end와 적용된 오디오 시각이 맞는지 확인하고, 2초 대기 상한을 넘기면 경고와 함께 원본 경로가 유지되는지 검사한다. 보관된 오디오를 방출할 때 STT 입력 큐가 정상 구간을 버리지 않는지도 확인한다.

DF 사용/미사용/재사용을 교대로 만든다. 미사용 구간에서 `processHop` 호출이 0회인지 확인하고, 경계에서 이전 원본 tail이 시각 순서대로 한 번만 출력되는지 검사한다. 추론을 건너뛰는 동안 recurrent state를 유지한 것처럼 처리하지 않으며, 재시작 시 초기화에 따른 품질·추가 비용을 측정한다. 첫 모델 로드 비용과 구간 추론 비용을 구분한다.

## 5. 지연·장시간

입력 타임스탬프와 STT partial/final, chunk 확정, 번역 표시, diarization 결과, DF 출력, separator 완료 시각을 같은 기준으로 기록한다. 평균만 보지 말고 p50/p95 및 최악 지연을 비교한다. 현재 설정 화면은 이 전체 지연 기록기를 구현한 상태가 아니다.

30분 예비 검사 후 2시간 이상 연속 실행한다. 기본 STT/번역과 overlap 빈도가 높은 자료를 나누어 메모리, thermal state, 배터리 변화, 자막 처리 지연을 기록한다. 화면 밝기·YouTube 재생 조건을 맞춘 비교가 필요하다. 측정 장비 없이 배터리 %만으로 정확한 W 수치를 보고하지 않는다.

첫 기본 기능 확인 후, 1~3시간 endurance 검사 전에 caption ID 인덱스와 mutation 경로를 개선한다. 현재 반복 선형 검색과 계산 속성을 통한 배열 변경 비용은 미측정이며 최적화도 미완료다. 수천/수만 행에서 번역·형태소 반영 및 스크롤 비용을 계측하고 비교한다. 이력을 임의로 잘라 성능 문제를 감추지 않으며, 행 ID/순서/번역/팝업/저장 문맥을 유지해야 한다.

화자 모델/음성 개선 모델/DeepL/단어장 네트워크 오류를 각각 유도해 기본 자막이 계속되는지 확인한다. enhancement 오류 경계의 짧은 누락, 장시간 transcript 메모리 증가는 별도로 확인한다.

## 결과 기록 형식

각 검사에 날짜, OS/앱 빌드, 입력 자료, 실제 결과, 성공/부분 성공/실패/미검증, 재현 절차를 남긴다. “기대되는 동작”을 “실제 성공” 칸에 쓰지 않는다.
