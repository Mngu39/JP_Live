# 현재 기준 상태 — checksum-preflight-2026-09-12

**상태: STT 입력 시간축 수정 소스. GitHub Actions는 체크섬 목록의 CRLF 때문에 Apple 컴파일 전에 실패했다. 이번 수정은 체크섬 생성·검증과 Git 줄바꿈 정책이며 Swift 앱 소스는 바꾸지 않았다. 수정 후 Apple 빌드·XCTest·iPad 실행 성공은 아직 확인되지 않았다. 사용 가능 판정 및 iPad 재실행 요청을 보류한다.**

이 문서가 현재 상태의 기준이다. 다른 검토 문서의 예전 파일 수·테스트 수·성공 지점은 해당 시점의 기록이다. 설정 화면의 소스 버전으로 이번 사본을 구별할 수 있다.

## 2026-09-12 CI 체크섬 수정

- 앱 전용 저장소는 `Mngu39/JP_Live`이다. 기준 커밋 `c7aca18e7d9ca00c4bfdc549d0e158fa15e57e66`의 GitHub ZIP 75개 파일은 수정 전 전체 배포 ZIP과 바이트 단위로 모두 일치했다. 누락된 프로젝트 파일이 원인이 아니다.
- 기존 `SHA256SUMS.txt` 74개 레코드 모두 CRLF였다. Windows의 텍스트 쓰기가 LF를 CRLF로 변환했고, macOS `shasum -c`가 CR을 파일명 일부로 읽었다. [Phase 2 실행 #5](https://github.com/Mngu39/JP_Live/actions/runs/34691164586/job/103546544577)에서 실행 중인 `Tools/test-apple.sh`까지 없는 파일로 판정했다. **Apple 컴파일·XCTest 진입 전 실패**이며 Swift 오류로 집계하지 않는다. Phase 1의 동일 실패는 사용자 보고다.
- `Tools/source_checksums.py generate .`는 원본 파일 바이트를 SHA-256으로 계산하고 목록을 바이너리 쓰기로 LF 저장한다. Windows 배포 도구도 이 생성기를 사용한다.
- `Tools/test-apple.sh`는 원본 목록을 보존하고, CRLF 레코드 경계만 LF로 바꾼 별도 목록을 `shasum -c`에 전달한다. 해시 값·소스 파일·파일명 내부 CR은 바꾸지 않는다. 실제 소스 변경이나 누락은 실패한다. 검사 실패 로그도 `BuildOutputs`에 남긴다.
- `.gitattributes`는 일반 파일의 Git 줄바꿈 자동변환을 막고 shell/Python/YAML/체크섬 목록에 LF를 명시한다. 소스 내용 자체를 정규화하여 검사를 우회하지 않는다.
- 체크섬 회귀 테스트 8개를 Windows Python에서 실행해 통과했다. LF·CRLF·혼합 목록, 소스 변경·누락·소스 줄바꿈 변경 시 실패, 잘못된 레코드, 생성 제외 경로를 확인했다. **Swift XCTest 72개의 실행 결과와 별개**다.
- 수정본 업로드 뒤 Phase 1·2를 새 커밋으로 실행하여 실제 macOS 체크섬 검사와 Apple 컴파일 진입 여부를 확인한다. 이 문서 작성 시점에는 수정 후 CI 결과가 대기 상태다.

## 실제 기기에서 확인된 이력

사용자가 이번 수정 **이전** 사본을 iPad Swift Playground에서 실행한 보고다. 실행한 OS·Playground·SDK의 정확한 빌드 번호 및 입력 파일 형식/샘플레이트는 아직 기록이 없다.

| 단계 | 확인 결과 |
|---|---|
| 최초 외부 의존성 resolve | NemoTextProcessing ZIP을 풀 `unzip`이 없어 실패 |
| Phase 1 외부 의존성 제거 후 resolve | 성공 |
| 그다음 Swift 컴파일 | LanguageTools의 CFLocaleIdentifier, StringTransform 추론, Bundle.module에서 실패 |
| LanguageTools 수정본 Swift 컴파일 | 성공 — 사용자 보고 |
| 앱 실행·파일 가져오기 | 성공 — 사용자 보고 |
| Apple STT 준비·오디오 투입 시작 | 도달 — 사용자 보고 |
| STT 스트리밍 | **실패: `Audio input timestamp overlaps or precedes prior audio input`** |
| STT→번역→팝업→일본어 저장 완주 | 확인되지 않음 |
| 실제 JSON 리소스 해석·단어장 연동 | 별도 성공 보고 없음 |

기기에서 컴파일에 성공한 기록은 이번 변경 코드의 컴파일 성공을 뜻하지 않는다. 이전 ZIP의 마지막 SHA-256은 전체 `f7a1477add2b39b754c8b915b482e8cfffda9a532a80607416b834082f3d0057`, Playground `1ecbf9600348f294e0ef86add6662005bf506085858fd5d05c833fbd07755f4c`였다. 사용자 기기 사본의 해시를 직접 대조한 것은 아니다.

## 이번에 수정한 범위

- 기존 STT 입력은 변환 전 `chunk.time`을 변환 후 버퍼에 붙였다. 이제 Phase 1은 변환된 샘플 수를 정수로 누적하여 `CMTime`을 만든다. 구간 원점도 출력 샘플 단위로 한 번 변환하여 서로 다른 timescale의 반복 반올림으로 나노초 단위 겹침이 생기지 않게 했다. epsilon 추가나 이전 end에 clamp하는 보정은 없다.
- 앞단의 원본→48 kHz 변환에도 같은 출력 샘플 시계를 적용했다. 변환기의 요청 크기만큼 입력을 한 번씩 공급하고, 나오는 모든 버퍼를 처리한다. 임시 입력 부족과 실제 EOF를 구분하고, EOF에서 남은 출력도 받아 분석/증폭/STT에 순서대로 전달한다.
- 원본 시각은 해당 샘플 격자에서 누적 프레임 수와 비교한다. 반 샘플 미만의 표현 오차는 같은 샘플로 해석한다. 한 샘플 이상의 겹침/역행은 오류이며, 실제 gap 또는 형식 변경은 이전 구간을 마감한 뒤 새 구간으로 처리한다. 종전의 50 ms 경계 판정을 제거했다.
- Phase 2는 `AnalyzerInputConverter`의 `convert(_:at:)`와 `flush()`를 사용한다. 이 API는 공식 문서상 iOS/iPadOS 27 이상이므로, iOS 26을 지원하는 Phase 1 컴파일 분기에는 참조하지 않는다. 구간 첫 입력에만 원본 시각을 주고 연속 입력에는 `nil`을 준다. 반환된 입력의 시각도 Analyzer에 넣기 전에 검사한다.
- 파일 EOF와 정상 중지는 이미 수용한 입력을 끝까지 내보낸다. 처리 실패 시에는 실패한 STT에 추가 입력을 보내지 않고 정리한다. 종료 오류를 `try?`로 무시하거나 성공/중지 문구로 덮지 않는다. 입력 취소가 마지막 정리 작업까지 취소하지 않도록 별도 Task에서 정리한다.
- 파일 읽기가 진행하지 않는 경우 무한 반복하지 않는다. 재시작과 언어 변경은 이전 실행 정리를 기다린 뒤 새 pipeline·STT converter·capture ID를 사용한다.
- ScreenCaptureKit에서 공유 대상을 다시 선택해도 최초 PTS 원점을 보존한다. 선택 중 공백을 지워서 앞 오디오 바로 뒤에 붙이지 않는다. 유효하지 않은 PTS나 역행은 표시하고 중단한다.
- 화자 분석은 실제 변환된 48 kHz PCM과 같은 원점을 사용한다. 구간을 마감할 때 resampler tail→분석 EOF→대기 PCM/개선 tail→STT flush 순서를 유지한다. 종전의 `rememberAudio`에서 역행을 가리던 timeline 초기화를 제거했다.

원래 오류 당시 입력·출력 프레임 수를 수집한 로그는 없으므로, 변환기 priming만이 유일한 원인이었다고 확정하지 않는다. 확인된 시간표 부착 계약 위반과 관련 누락 경로를 수정한 것이다.

## 검사와 미검증을 구분

| 검사 | 이번 사본 결과 |
|---|---|
| tree-sitter 문법 검사 | Swift 38개, Rust 1개, Python 2개 통과. Swift 타입 검사 아님 |
| 소스 복사·리소스·프로젝트 연결 등 정적 확인 | 16개 항목 통과 |
| 새 오디오 회귀 XCTest | 17개 추가, 기존 55개와 합계 72개. **실행 0개** |
| Apple compiler / XCTest / 기기용 build | **미실행** — 현재 Windows 환경에 Apple SDK와 연결된 Mac 호스트 없음 |
| 이번 사본 iPad runtime | **미실행** |
| CI | 앱 전용 저장소에 업로드·실행됨. 수정 전 CRLF 체크섬 목록에서 실패했으며 Apple 컴파일·XCTest는 미진입. 위 2026-09-12 수정 후 결과는 대기 |

새 XCTest는 실제 AVAudioConverter/AnalyzerInput을 사용한다. 샘플레이트 8/16/22.05/32/44.1/48/96 kHz, 불규칙 버퍼, 분할 크기별 출력 비교, impulse 위치/끝부분, 짧은 EOF, double flush, 10 ms STT 연속 입력, 소수 원점, 실제 gap/역행, 두 변환 단계, 파일 EOF·중지·재시작을 다룬다. 배열/시계의 별도 JavaScript 흉내를 Swift 실행 결과로 인정하지 않는다.

현재 로컬 tree-sitter의 Bash/YAML 모듈은 새 파일 검사 중 내부 오류를 냈다. 새 CI 스크립트의 Bash/YAML 파서 통과를 주장하지 않는다. Python 시뮬레이터 선택 도구의 실행 검사는 별도 기록한다. Apple CI 자체가 실행되어야 build/test 스크립트의 실제 작동을 확인할 수 있다.

다음 관문은 **Apple compile→네이티브 회귀 테스트→기기용 build**다. 그다음 한 번의 기기 확인 묶음으로 일본어/영어 파일 EOF까지 STT→번역→문장/단어 팝업, 중지/재시작·언어 변경을 확인한다. Simulator 변환 테스트만으로 STT 모델 또는 번역/시스템 캡처의 실기 동작을 통과 처리하지 않는다.

## 자동 빌드 준비

- `Phase1/JPLive.xcodeproj`: 외부 패키지 없이 Playground와 같은 Shared 분기·리소스를 컴파일하는 Xcode 앱/test host. iOS 26 이상. Playground 포장 자체의 검증은 대신하지 못한다.
- `Phase2/JPLive.xcodeproj`: 기존 FluidAudio·DeepFilterNet 고정 의존성·iOS 27 캡처를 유지한다. 같은 오디오 회귀 테스트도 연결했다.
- Mac에서 `bash Tools/test-apple.sh 1` 또는 `2`: 입력 소스 해시 확인, SDK 확인, 사용 가능한 iPad simulator 선택, XCTest, Release 기기용 무서명 build. 로그·xcresult·툴체인·소스 해시를 `BuildOutputs`에 남긴다. 어느 단계가 실패해도 PASS 파일을 만들지 않는다.
- `Tools/build-macos.sh`도 archive/IPA 생성 전에 Phase 2의 위 테스트를 실행한다. IPA 단계에서 처음 컴파일 오류를 발견하는 흐름을 허용하지 않도록 연결했다. 이것 역시 Mac에서 실제 실행한 기록은 아직 없다.
- `.github/workflows/apple-validation.yml`: JP-Live 폴더 내용을 **앱 전용 저장소 루트**에 둘 때 수동 실행할 workflow다. Phase 1은 `macos-26`, Phase 2는 공식 preview `xcode-27` runner를 사용한다. 기존 사진번역기 저장소/Worker에는 적용하지 않았다. 배포·서명·IPA 설치는 수행하지 않는다.

## 유지해야 하는 요구사항

Phase 1에서 FluidAudio·DeepFilterNet·시스템 캡처를 제외한 것은 Playground host 제약 때문이며, 최종 앱 요구사항에서 삭제한 것이 아니다. Phase 2에는 FluidAudio 화자 분석, DeepFilterNet 선택적 음성 개선, ScreenCaptureKit 시스템 오디오가 남아 있다. 기본 STT가 선택 모델 다운로드를 기다리지 않는 시작 순서도 유지했다.

일본어/영어 모두 STT·한국어 번역·gutter 문장 번역·형태소/단어 클릭 번역 대상이다. 단어장·일본어 학습·기존 사진번역기 방식의 저장/로그/AI 재구성은 일본어만 대상이다. 기존 Worker/DB/GitHub 사진번역기 코드를 변경하거나 배포하지 않았다.

아직 미완료인 원래 항목: Sudachi native 연결, 실제 live 2화자 separation·stem별 VAD/leveling·2STT 합류, diarization과 별개인 입력 normalization 및 볼륨 비교 측정, 긴 전사 성능 개선, 영어 단어 번역의 문맥 계약 확장, 한 STT 결과 안의 단어 단위 화자 경계 처리. 이번 시간축 수정으로 이 항목들이 완성된 것은 아니다.

## 근거

- [Apple AnalyzerInput 시각 계약](https://developer.apple.com/documentation/speech/analyzerinput/init(buffer:bufferstarttime:))
- [Apple AnalyzerInputConverter — iOS/iPadOS 27+](https://developer.apple.com/documentation/speech/analyzerinputconverter)
- [Apple convert(_:at:) — 보류 입력과 flush](https://developer.apple.com/documentation/speech/analyzerinputconverter/convert(_:at:))
- [Apple iOS 26 SpeechAnalyzer 예제](https://developer.apple.com/documentation/speech/bringing-advanced-speech-to-text-capabilities-to-your-app): BufferConversion.swift의 primeMethod `.none` 방침 확인. 기존 샘플 경계의 필터 품질을 일부 희생하며, 내부 보류 출력은 별도로 마감해야 한다.
- [GitHub macOS 26 runner](https://github.com/actions/runner-images/blob/main/images/macos/macos-26-Readme.md), [Xcode 27 preview runner 공지](https://github.com/actions/runner-images/issues/14404)
